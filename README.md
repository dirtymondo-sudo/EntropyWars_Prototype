# DOOR HQ reference art — drop zone

Put the facility reference images here (PNG or JPG, ≤ 2 MB each) so future
Claude sessions can open them (Claude's file reader displays images
committed to the repo). Names expected by `DOOR_HQ_BUILD_PLAN.md` §5.1:

- `central_egress_v1.*`   — the round two-tier hall with the hanging cube
- `office_doors_sheet_v1.*` — the six office doors (rank ladder)
- `training_room_v1.*`    — the top-down 8×8 grid room
- `janitor_closet_v1.*`   — the L1 office
- `concept_board_v1.*`    — the ChatGPT board (ring map, door states, rings)

New rooms: `<room_id>_v<N>.*` (room ids from `DOOR_HQ` in data.js).
These are REFERENCES for planning and hotspot drafting. The production
backgrounds the game actually loads go to the R2 bucket under
`Assets/door/hq/` (spec in the build plan §5.2) — not here.
