# PARTY BUILDER REDESIGN — THE FORGE TERMINAL
### The vessel-assignment screen becomes one CRT monitor · rev 6 (2026-09-09 — Stages 1–5 SHIPPED: the monitor, the tabs, the row, the circuit, the technique panel, the move preview, the VFX on the stage, the hero that CHARGES + the camera that frames it, the ROSTER wall, the STICKY NOTES on the bezel, the stat pills, the affinity ring, the GEAR wheel; Stage 6 next)

Read CLAUDE.md first (RULE #1 delivery — full files in chat, never a patch;
#1b the `?v=` bump in index.html with EVERY R2 file; #1c no playtest unless
asked; #2 online parity). This file is the step-by-step plan for rebuilding
the party builder (`party-builder.js`) around the seven reference images in
`party_builder_references/` (committed 2026-09-09 — Claude can view them
with the Read tool). It is the anti-"start over" memory for the builder:
what to build, in what order, in which EXISTING files, and what is decided
vs. still the user's call. **Append to §9 (build log) every session that
touches the builder.**

---

## 0. The brief (the user's words, 2026-09-09)

> Reference photos for a new party builder, specifically the part where you
> select your spells. Selecting a champ (the smash bros / mortal kombat like
> grid) and selecting the spells should be separate — different tabs of the
> same screen. It should be diegetic with the computer monitor. The passives
> or something can be sticky notes. The party (currently a column on the
> left) should be a row on the bottom. Circles or rounded corners instead of
> squares where possible. The hero unit should actually play its spell
> animations and VFX when you are selecting a spell. Do it in stages.

Everything below serves those seven sentences.

## 1. What the references say (read them once, then build from this)

Seven images, one screen: a **CRT monitor** (bezel, glass, scanlines, a
paper label on the frame) running a **character terminal**.

| What                         | How the references do it                                                                                                                                                                                                                       |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Tabs**                     | A bar across the top of the glass: `[L1] STATUS · TECHNIQUES · EQUIPMENT · DOSSIER [R1]` (U.N.A.), `SKILLS · GEAR · LORE` (Bigfoot), `STATUS · LOADOUT · SKILLS · ARCHIVE · SYSTEM` (Nexus Veil). Shoulder buttons cycle them.                    |
| **The hero**                 | Full-height in the middle or right third, lit, in front of a mood backdrop. Aya Voss shows the **MOVE PREVIEW**: the hero mid-animation with the technique's slash drawn across the frame.                                                       |
| **The technique tree**       | Three **pillars** (PSI / EXOGENESIS / WARFARE; TIDES / SONG / SCALES; FLESH / MIND / ABYSS), each with a title, a one-line tagline, a lit ring-1 node and three dimmer nodes below, joined by a vertical line. Nexus Veil and Aya Voss draw the nodes as **circles**; Aya Voss curls the whole tree into a ring. |
| **The technique panel**      | Bottom-left, under the tree: big icon · `PSI LV 1: TELEKINESIS` · one-line description · `AP COST 1` · the verb (`PURCHASE` / `LEARN` / `CONFIRM`). Every reference has this panel.                                                                 |
| **Stats**                    | A right-hand block. Nexus Veil / Aya Voss: each stat is a **round icon disc** + label + a rounded bar + the number ("pills"). Plus a RESISTANCE / AFFINITIES row of six **circular type icons** (NUL / RES / WKN).                                     |
| **The party**                | A **row along the bottom** of the glass: circular portraits, the active one larger and ringed, `◂ ▸` caps (L1 / R1), names under, `PARTY 4/6`. Nemesis-OS has the same row as square film-strip frames.                                            |
| **Sticky notes**             | Paper notes stuck on the **bezel** (outside the glass), slightly rotated, handwritten: `PASSIVE: SECOND WIND — Recover 8% HP after a perfect evade.` (pink, top-right) · `Passive: Sixth Sense — Slightly increases evade vs. ambush.` (yellow, bottom-left). |
| **Chrome**                   | An OS header (`NEMESIS·OS v2.3.1 · OCCULT ASSET MANAGEMENT TERMINAL`), a motto on the right, a paper label on the bezel (`PROPERTY OF THE UNSEEN`), a barcode, a power LED, `SELECT · CONFIRM · BACK · VIEW INFO` button legend.                        |

What the game already has that matches this: **THE TERMINAL** (match-select
is a CRT — `.ms-crt` in styles-base.css, match-select.js renders bezel →
glass → screen), the D.O.O.R. paper/stamp language (`door-stamp`,
`door-file-*`), the DOOR synth kit (`playDoorSfx('crtOn' | 'vhsEject' |
'stamp' | 'dotMatrix')`), the phosphor palette (`--ph-*`), and a live 3D
hero stage (`EWCharViewer`). The builder does not need a new look; it needs
to be moved INTO the look the match-select screen already wears.

## 2. What exists today (the anatomy — read before editing)

**party-builder.js** (2 821 lines, React via `React.createElement`, one
IIFE, injects its own `<style id="pb-hover-css">`). One component,
`PartyBuilder()` (line ~1595), mounted two ways:

- **Pre-match**: ui.js `renderBuilder()` (~583) → `window._mountReactPartyBuilder()` → `#builderOverlay` (index.html, INSIDE `#battlePanel`). `backToModeSelect` (map.js) leaves it. Online: the same component; `window._NET` carries `role`, `_lockState`, `_waitingForOpponent`, `ranked`; the foot shows CONFIRM / SEAL / WAITING / START MATCH from those flags.
- **Standalone** ("PARTY BUILDER" on the main menu, the Quartermaster door's `alt` in the HQ): map.js `_goToTeamBuilder` → `_showTitlePage('teamBuilderPage')` → `window._mountReactTeamBuilder()` → `#teamBuilderBody`; `_pbStandaloneMode` = true; opens on the TEAM ARCHIVE locker (`tbView === 'locker'`), FORGE edits a preset, SAVE TEAM writes `profile.teamPresets`.

Today's layout (all inline styles): header bar (46px) → 3-column grid
`112px | 1fr | clamp(380px,36vw,640px)`:
- left = **THE PARTY** column (slot cards with `PortraitSprite`, FILED stamp, numeral);
- centre = **hero showcase** (54% height: gear rail · `HeroViewer3D` · item rail; the assessment sheet with ASSESSMENT / DOSSIER tabs, `VitalBar` / `StatBar` / `RangeDiamond` / `RACE_TRAITS`) over the **Codex of Vessels** roster grid (search, sort, type / job / faction filters, ★ favourites, 🔒 shop gate);
- right = **Abilities**: `SpellTreePanel` (tree classes) or the flat pool / 6-slot rack fallback; twin / wildcard node picker overlay.
→ command bar (56px): BACK · 🎲 ONE · 🎲 ALL · RESET · ★ SAVE · ↑ LOAD · CONFIRM n · SEAL YOUR FATE.
Overlays: team save/load modal, equip picker (`subjob` / `item` / `accessory1|2`), the locker.

State the builder writes (unchanged by this plan): `state.partyBuilds[p][i]`
(job), `state.partyMeta[p][i]` (`race`, `gender`, `zodiac`, `secondaryJob`,
`customSpells`), `state.partyNames`, `state.loadouts[p][i].{items,equipment}`,
`state.builderSelectedSlot/Player`, `state.builderConfirmedSlots`,
`state.teamLockedIn`, `state.showPlayer2Builder`.

**The spell tree** — `SpellTreePanel` (~1211): `TREE_NODE_POS` puts `root`,
`R1–R4` (race pillar, centre), `P1–P4` (primary job, left), `S1–S4`
(subclass / wildcards, right) in a 100×100 space; edges are SVG `<line>`s
under opaque 46px **circular** chips; state per node = root / socket /
empty / equipped / reachable / far / sealed / blocked
(`computeTreeEquipPath`, `treeReachableKeys`, `isTreeLoadoutLegal`,
`treeLegalSubset`); twin nodes (`tree.alts`, ⇄ badge) and Freelancer
sockets (`tree.sockets`) share one picker overlay. Category colours =
hud.js `_HRLG_CAT` (red damage / green heal / blue buff / purple debuff /
gold utility). None of this logic changes; only its skin does.

**The 3D hero** — `EWCharViewer` (three-renderer.js ~28440–29108,
`window.EWCharViewer`): a SINGLETON second WebGL context; `mount(host, race,
gender, { accent })`, `setCharacter`, `unmount`, `resetView`, `isMounted`,
`supports`. It loads the race's `RACE_MODELS_3D` GLB through the same cache
as the board (`_loadUnitGLB`, `_cloneUnitModel`), bakes the shared animation
library onto it (`_animLibBakeForModel` → `baked[slot]` for EVERY
`def.libClips` slot: idle, walk, castMagic, castRanged, castMelee, castAOE…
see sprites.js `UAL_SLOTS`) and today plays only `baked.idle`. Drag = yaw,
wheel = zoom, dblclick = reset. `HeroViewer3D` (party-builder.js ~806)
mounts it into `.pb-hero3d` with the 2D sprite as the loading frame
(`ew-cv-loading / ready / fail` host classes, styles-hud.css).

**How the board plays a cast** (what the preview must mirror): battle.js
`triggerCastAnim(unit, spell)` → `state._castAnimKind[uid] =
classifySpellAnimKind(spell)` (sprites.js — 'magic' | 'support' | 'ranged' |
'melee' | 'throw' | 'plant' | 'heal' | 'aoe' | 'slam' | 'arrow' | 'kick' |
'punch' | 'claw' | 'consume' | 'deploy') → three-renderer.js ~18589 turns
the kind into a **fallback chain** (`['castMagic','cast']`,
`['castSlam','castAOE','castMelee','cast']`…) → `_maybeStartModelAnim(uid,
chain)` → `_playUnitModelAnim` (reset · play · `crossFadeFrom(prev,
MODEL_ANIM_FADE)`), one-shot for `min(clip.duration / timeScale, 1.4 s)`,
then the state machine returns to idle. Basic attacks:
`def.basicAttackKind` (sprites.js) → castMelee / castRanged.

**How the board draws a spell's VFX**: battle.js `_stageSpellCast` /
`_fireStageBeat` → `window.VFX3D.fire(intent, spellId, params)`
(three-vfx-effects.js ~3485). Intents: `windup` / `burst` / `finish`
(the data-driven STAGING beats — every spell has them, resolved from
cost / tier / kind / element, no SPELL_MAP entry needed) and the mapped
ones from `SPELL_MAP[spellId]` (`impact`, `aura`, `bolt`, `beam`, `aoe`,
`descent`, `wall`, `chain`, `teleport`). Particles are spawned by
three-vfx.js `ThreeVFX` (one `_scene`, set once by `init(scene)` from the
battle renderer; pooled sprites / quads / globs; `tick(dt)` from the battle
loop). Positions come from `tilePx(tx, ty)` (CONFIG tile maths, px),
`unitSurfaceZ` (`ThreeRenderer.tileTopY` when the board is active) and
`unitZBoost`. `_suppressed()` refuses everything unless `state.phase ===
'battle'`. **online.js WRAPS `VFX3D.fire` to relay host → guest** (~2176) —
a builder preview must never go through the wrapped `fire`.

**The Spell Lab** (ui.js ~9516, Settings → Developer → Spell Library → ⚗)
is the existing "play the real cast" tool: it boots a private 8×8 match and
free-casts through `GAME.doSpell`. It is the reference for what a preview
should look like, not the mechanism (it takes over `state` and the battle
renderer).

**Passives**: data.js `PASSIVE_DEFS[id]` = `{ id, icon, name, desc, …hook
fields }`; `RACE_PASSIVES[race]` = up to two ids; `getUnitPassives(unit)`
adds `flying` first for any unit `canFly` says flies, capped at
`MAX_UNIT_PASSIVES`. The builder also carries `RACE_TRAITS` (party-builder.js
~1409): a hand-written table of terrain rules / design-intent passives per
race (`{ icon, name, desc }`), some `CODED`, some not — today's "RACE
TRAITS · PASSIVES & TERRAIN" list on the assessment sheet.

**The CRT chrome** (match-select.js ~676, styles-base.css "THE TERMINAL"
block ~4210): `.ms-crt.ms-crt-<frame>.ms-crt-<variant>` → `.ms-crt-bezel`
→ `.ms-crt-glass` → `.ms-crt-screen` → `.ms-tty` (head / body / foot) +
`.ms-crt-scan` + `.ms-crt-glare`; `.ms-crt-label` (the paper label),
`.ms-crt-brand`, `.ms-crt-led`. Phosphor vars on `.ms-crt`: `--ph`,
`--ph-dim`, `--ph-faint`, `--ph-gold`, `--ph-teal`, `--ph-green`,
`--ph-red`, `--ph-line`, `--ph-line-hi`, `--ph-glow`. Power-on / off =
`.hq-terminal.on / .off` (`msCrtOn` / `msCrtOff`). Two homes: `#modePage`
(`frame: 'page'`, the monitor on black) and `#hqTerminal` (`frame: 'room'`,
the camera pushed onto a desk's `crt_terminal` prop by
`ThreeRenderer.hq.focusScreen({ counterId })`, map.js `_hqOpenTerminal`).
The match-select IIFE does not export the chrome; the builder renders the
same class structure itself (the CSS is shared).

**Tests that read party-builder.js**: champ-rework.test.js ("Phase 6")
asserts `'gangster': '` and `'nun': '` appear in the source (the
`CODEX_LORE` rows). Keep `CODEX_LORE` and its shape. `npm test` also
syntax-checks every repo JS. Add `party-builder.test.js` in Stage 1 (§5.1).

## 3. The design rules (apply in every stage)

1. **One monitor.** The whole builder lives inside `.ms-crt`. Nothing floats outside the bezel except the sticky notes (§5.5), which are ON the bezel. Overlays (pickers, modals, the locker) are windows drawn on the glass, not browser modals.
2. **The rounding rule.** New chrome is round: `--pb-r: 16px` panels, `--pb-r-sm: 9px` rows, `999px` pills for buttons / chips / tabs, `50%` for portraits, tree nodes, stat discs, type icons, gear slots. The only squares that stay: the D.O.O.R. rubber stamps (`door-stamp`), the paper label, the sticky notes (paper is square), and the MOVE / RANGE footprint diamonds (they ARE the shape of the rule they explain — put them inside a round badge).
3. **Phosphor first, faction second.** The chrome keeps the D.O.O.R. terminal's warm phosphor (`--ph-*`); the selected vessel's **faction colour** (`--pb-fc`, already on the root) tints the active tab, the party ring, the technique panel edge, the summoning circle. Category colours (`_HRLG_CAT`) stay on tree nodes — same coding as the battle action menu.
4. **Type = font + case.** `DotGothic16` for terminal text (uppercase, tracked), `Cormorant SC` for names and headings, `IBM Plex Mono` for the prompt line. Sticky notes get one handwriting face (decision C-6).
5. **Every mechanic keeps its function name.** `pickRace`, `toggleSpell`, `treeNodeClick`, `twinPickSpell`, `flEquipWildcard`, `handleSecJobChange`, `equipAccessory`, `setItemCount`, `confirmSlot`, `doStart`, `saveTeamAs`, `loadTeamPreset`, `tbSaveTeam` — the redesign re-skins, it does not re-implement. If a stage needs to change a rule, it says so in its list.
6. **RULE #2 in the builder.** The builder is local UI (nothing relays), but: (a) the online lock / waiting / start flow in the foot must survive every stage exactly (`isWaitingOnline`, `opponentLockedToo`, `friendlyHostCanStart`); (b) the stage preview must bypass the relayed `VFX3D.fire` (§5.3); (c) `state.partyMeta` shapes are what `applyPartyBuild` and the host's `treeLegalSubset` validation read — untouched.
7. **RULE #1c.** No Playwright. Validation per stage = `node --check` + `npm test` + the source-scan test + reading the render code. The user looks at it.
8. **Mobile survives.** `window.EW_MOBILE` / `@media (max-width: 1180px | 940px)`: the bezel thins, the stats column drops under the stage, the party row scrolls, the tree scrolls. Nothing may require hover.

## 4. The screen (target anatomy — every stage fills a zone)

```
┌ BEZEL ──────────────────────────────────────────────────────────────────────┐
│ ┌ GLASS ─────────────────────────────────────────────────────────────────┐  │
│ │ HEAD  ENTROPY WARS · D.O.O.R. CUSTOMS & ADMISSIONS · VESSEL ASSIGNMENT  │  │
│ │       [officer chip]                      ARENA · 4 SLOTS · ESC hint   │  │
│ │ TABS  [L1]  ( ROSTER )  ( TECHNIQUES )  ( GEAR )  ( DOSSIER )   [R1]   │  │
│ │ ┌ TECH ───────────┐ ┌ STAGE ───────────────────┐ ┌ STATS ───────────┐  │  │
│ │ │ pillar  pillar  │ │      (MOVE PREVIEW)      │ │ ● HP  ▬▬▬▬▬▬ 640 │  │  │
│ │ │  ◯ ─── ◯ ─── ◯  │ │                          │ │ ● MP  ▬▬▬  120  │  │  │
│ │ │  ◯     ◯     ◯  │ │        the hero          │ │ ● ATK ▬▬▬▬  78  │  │  │
│ │ │  ◯     ◯     ◯  │ │      (EWCharViewer)      │ │ ● DEF ▬▬    44  │  │  │
│ │ │  ◯     ◯     ◯  │ │                          │ │ …               │  │  │
│ │ │ ┌ TECHNIQUE ──┐ │ │      ◇ MOVE   ◇ RANGE    │ │ AFFINITIES       │  │  │
│ │ │ │ ◯ RING 2 ·  │ │ │                          │ │ ◯ ◯ ◯ ◯ ◯ ◯      │  │  │
│ │ │ │ FIREBALL    │ │ └──────────────────────────┘ └──────────────────┘  │  │
│ │ │ │ desc · cost │ │                                                    │  │
│ │ │ └─ [ EQUIP ] ─┘ │                                                    │  │
│ │ └─────────────────┘                                                    │  │
│ │ PARTY ROW  ◂  (I)  (II)  (III)  (IV)   ▸        P1 | P2      4 / 4     │  │
│ │ FOOT  ◂ BACK  SLOT II · name · race · job    > forge --slot 2   [SEAL] │  │
│ └────────────────────────────────────────────────────────────────────────┘  │
│  ▣ PROPERTY OF D.O.O.R. · FORGE-1 · DO NOT UNPLUG      ENTROPY DATA  ● LED │
└──────────────────────────────────────────────────────────────────────────────┘
   ┌ STICKY ┐ (on the bezel corners)                            ┌ STICKY ┐
   │PASSIVE:│                                                   │TERRAIN │
```

Per tab the body grid changes; the STAGE (the hero) and the STATS column
stay on TECHNIQUES / GEAR / DOSSIER; ROSTER swaps the tree for the champ
grid and slides the stage to the right third (the Union Arms layout).

| Zone      | Tab       | Content                                                                                                 |
| --------- | --------- | ------------------------------------------------------------------------------------------------------- |
| TECH      | ROSTER    | the champ-select grid (full width minus the stage)                                                      |
| TECH      | TECHNIQUES| the three-pillar circuit + the technique panel                                                          |
| TECH      | GEAR      | gear circles · item circles · subclass pill · name · zodiac wheel                                        |
| TECH      | DOSSIER   | the D.O.O.R. customs file (lore, disposition, point of entry, annotation)                                 |
| STAGE     | all       | the hero; MOVE PREVIEW pill; the summoning circle; on ROSTER a hover-preview of the hovered vessel        |
| STATS     | all but ROSTER | vitals + stat pills + MOVE/RANGE + AFFINITIES; on ROSTER a 4-stat quick read under the hero           |
| PARTY ROW | all       | circular portraits, active ring, FILED badge, `◂ ▸`, P1/P2 (local only), `n / size`                      |
| FOOT      | all       | BACK · summary · prompt · CONFIRM n · SEAL / WAITING / START / SAVE TEAM                                  |

## 5. The stages

Each stage is one session's deliverable: the full edited files in chat +
the index.html `?v=` bump (RULE #1b) + `npm test` green + a §9 entry. The
order is deliberate: the skeleton first (everything else hangs on the
tabs), then the user's headline ask (the spell tab + the preview), the
riskiest tech (VFX in the stage) isolated in its own stage with a shipped
fallback behind it, then the champ grid, then the dressing, then the desk
in the building.

### 5.1 · Stage 1 — THE MONITOR: tabs + the party row (the skeleton)

**Goal.** The existing builder, re-homed into a CRT with four tabs and the
party as a row along the bottom. No mechanic changes. Every action reachable
before is reachable after.

**Files.** party-builder.js · styles-base.css · index.html (bump) · NEW
repo-only `party-builder.test.js`.

**Work.**
1. `PB_TABS = [{ id:'roster', label:'ROSTER', hint:'CHOOSE A VESSEL' }, { id:'tech', label:'TECHNIQUES', hint:'EQUIP ITS SPELLS' }, { id:'gear', label:'GEAR' }, { id:'dossier', label:'DOSSIER' }]` (module constant, on `window.PB_TABS` for the test). React state `pbTab`; `Q` / `E` and `[` / `]` cycle (the L1 / R1 caps in the tab bar are clickable too); `1–4` jump. Open on ROSTER; a `pickRace` flips to TECHNIQUES (decision C-1 — default yes).
2. The monitor: the root renders `.ms-crt.ms-crt-page.ms-crt-forge` → `.ms-crt-bezel` → `.ms-crt-glass` → `.ms-crt-screen` → `.ms-tty.pb-tty` + `.ms-crt-scan` + `.ms-crt-glare`; `.ms-crt-label` = `D.O.O.R. · CUSTOMS & ADMISSIONS · FORGE-1 · DO NOT UNPLUG`; `.ms-crt-brand` = `ENTROPY DATA SYSTEMS`; `.ms-crt-led`. `.ms-crt-forge` overrides in styles-base.css (a new "THE FORGE TERMINAL" block right after "THE TERMINAL"): a wider bezel top/bottom (room for the sticky notes later), `--pb-r` / `--pb-r-sm` tokens, the `pb-*` classes below. **Keep `.pb-tarot` on the root** (hover CSS keys off it) and `--pb-fc`.
3. HEAD (`.ms-tty-head`): `DoorSeal` · ENTROPY WARS · `D.O.O.R. · CUSTOMS & ADMISSIONS · VESSEL ASSIGNMENT` (standalone: `RECORDS · SQUAD MANIFESTS`) · `OfficerChip` · mode + `n SLOTS` · `[ESC] BACK`.
4. TABS (`.pb-tabs`): pills, active pill in `--pb-fc` with the phosphor glow, the hint under the bar (`CHOOSE A VESSEL` etc.), `[L1]` `[R1]` end caps.
5. BODY (`.pb-body[data-tab]`): a CSS grid per tab —
   `tech`: `clamp(330px,30vw,470px) minmax(0,1fr) clamp(240px,21vw,320px)`;
   `gear` / `dossier`: `clamp(300px,26vw,420px) minmax(0,1fr) clamp(240px,21vw,320px)`;
   `roster`: `minmax(0,1fr) clamp(300px,27vw,420px)` (grid | stage+quick stats).
   **The stage is rendered ONCE** (`.pb-stage`, stable React key, always mounted) and placed by `grid-area`; tabs change the template, never the element — `EWCharViewer` is a singleton canvas and must not remount per tab (the codex → builder handoff already relies on this).
6. Move the existing pieces into the tabs, unchanged inside: ROSTER ← the Codex of Vessels grid + its filter row; TECHNIQUES ← `SpellTreePanel` (+ the flat-pool / slot-rack fallback, the node picker overlay, the RND / RST / CLR tools); GEAR ← gear rail, item rail, subclass bar, NAME, ZODIAC; DOSSIER ← the customs file. STATS column ← `VitalBar` / `StatBar` / `RangeDiamond` / `RACE_TRAITS` (the traits list moves out in Stage 5).
7. PARTY ROW (`.pb-party`): `◂` cap · one `.pb-party-slot` per slot: a `border-radius:50%` portrait clip (`PortraitSprite`, `object-fit: cover`), a 2px ring in the slot's faction colour (active: 3px + glow + 1.12 scale), the numeral in a small disc top-left, the FILED stamp (`door-stamp door-stamp-sm admit`) bottom-right, name under, `RACE · JOB` under that · `▸` cap · `P1 | P2` toggle when `state.showPlayer2Builder` (local only; sets `builderSelectedPlayer`) · `n / size` counter. `←` / `→` move the selection; `selectSlot` semantics unchanged. Under 940px the row scrolls horizontally.
8. FOOT (`.ms-tty-foot`, the match-select classes): `◂ BACK` (`ms-tty-btn danger`) · `.ms-tty-sum` (`SLOT II · <name>` / `race · job · spells 5/6`) · the prompt `> forge --slot 2 --job "Black Mage"` · `.ms-tty-spacer` · the dice (`🎲 ONE` / `🎲 ALL` / `RESET`, `★ SAVE` / `↑ LOAD`) as small `ms-tty-btn`s · `CONFIRM II` · the primary (`ms-tty-btn primary`): SEAL YOUR FATE / ⌛ WAITING… / ⚔ START MATCH / 💾 SAVE TEAM — same conditions as today, verbatim.
9. Overlays → glass windows: `.pb-window` (rounded, `--ph-line` border, the phosphor black, a title strip, ✕): the team modal, the equip / subjob / item picker, the node picker. The standalone locker stays a full-glass view (`.pb-locker`) with rounded team cards.
10. `party-builder.test.js` (repo tooling, `node --test`): source-scans party-builder.js for `PB_TABS` with the four ids, the `.ms-crt-forge` root, `.pb-party`, one `.pb-stage`, the lock-flow names (`isWaitingOnline`, `opponentLockedToo`, `friendlyHostCanStart`, `doStart`, `confirmSlot`), the mechanic names in rule 3.5, and that styles-base.css carries `.ms-crt-forge` + `.pb-tabs` + `.pb-party`. Grows with every stage.

**Acceptance.** Pre-match VS CPU, online host + guest, standalone archive: every button works; keyboard tabs; the viewer survives tab switches without a reload; `npm test` green; index.html bumped.

**Risks.** `#builderOverlay` sits inside `#battlePanel` (a grid panel) — the CRT root needs `position:absolute; inset:0`. The old `.pb-*` CSS block at styles-base.css ~1106–3150 ("Character Select Screen v2") is dead legacy from an earlier builder — leave it (deleting is a separate cleanup; note it in §9 when confirmed unused). `EWCharViewer.unmount()` in `_unmountReactPartyBuilder` stays.

### 5.2 · Stage 2 — TECHNIQUES: the circuit, the technique panel, the move preview (animation)

**Goal.** The spell tab looks like the references (three pillars of glowing
circles with taglines, the technique panel under it) and the hero PLAYS the
cast animation when a technique is hovered or equipped.

**Files.** party-builder.js · styles-base.css · three-renderer.js
(`EWCharViewer.play` / `playSpell`, shared cast chain) · index.html (bump) ·
party-builder.test.js.

**Work.**
1. **The circuit.** `TREE_NODE_POS` stays (same keys, same legality). Connectors become SVG `<path>`s with `stroke-linecap: round`, lit segments in the phosphor, the hover path in gold, dashed for unlit. Chips: 50px circles (ring-4 capstones 58px with a thin outer ring instead of the ♛), the category glyph inside, the twin ⇄ as a satellite disc, sockets as dashed gold rings, sealed as a 🔒 disc in `--ph-faint`. Pillar heads become pills: title (`BLACK MAGE` / `<RACE>` / `SUBCLASS ▾`) + a tagline line under (`Discipline destroys.` style — decision C-5: Claude drafts the ~30 job + ~98 race taglines for approval, or the user writes them; until then the line shows the pillar's spell count, `4 TECHNIQUES`).
2. **Selection model.** `techSel` (node key) + `techHover`. Mouse: hover previews, click selects + does what it does today (`treeNodeClick` / `onTwinPick` / `onSocketClick`). Keyboard: `↑ ↓` along a pillar, `← →` across pillars, `ENTER` = click, `BACKSPACE` = unequip. The panel follows `techHover || techSel`.
3. **The technique panel** (`.pb-technique`, bottom of the TECH column): a 44px category disc with the glyph · `RING 2 · TIER II · DAMAGE` · the name (Cormorant) · desc · a chip row: `AP n` / `MP n` / `RNG n` / `AOE r` / `PWR n` (`pbPowerStat`) / the type badge with the matchup words (`pbTypeBadgeStyle`, `pbDmgIcon`) / status effects (`pbSpellEffects`) / `n SLOT(S)` · the verb pill: `EQUIP` / `UNEQUIP` / `⇄ PICK` / `＋ BROWSE` / `SEALED — <why>` / `PATH: +2 NODES` (auto-equips the path, as today). The hover tooltip (`buildSpellTooltip`) stays for the picker rows and the flat pool only.
4. **The picker** (twin / wildcard) as a `.pb-window` over the TECH column with rounded rows (`SpellBlade` restyled: `--pb-r-sm`, the slot numerals in discs).
5. **`EWCharViewer.play`** (three-renderer.js):
   - `_cvSetCharacter` keeps the WHOLE baked map: `v.clips = baked` (or the per-character `def.clips` table for `noAnimLib` defs), `v.actions = {}` built lazily via `mixer.clipAction`, idle as today.
   - `play(names, opts)`: `names` = a slot or a fallback chain; first slot with a clip wins; `LoopOnce`, `clampWhenFinished`, `timeScale` = the same per-slot scale the board applies (`def.libTimeScales[slot]`, else `UAL_SLOTS[slot].ts`), `reset().play()`, `crossFadeFrom(idle, 0.12)`; on `finished` crossfade back to idle. Returns the clip's ms (capped at 1.4 s like the board — `opts.full` lifts the cap for the preview). One preview at a time: a new `play` cuts the old one.
   - `playSpell(spell, def?)`: `classifySpellAnimKind(spell)` → the chain. **Extract the board's inline chain table (three-renderer.js ~18589) into `_castChainFor(kind)`** and use it from both sites so the preview can never drift from the battle. The root node (Basic Attack) uses `def.basicAttackKind` → `castMelee` / `castRanged`, like `triggerAttackAnim`.
   - `onState` callback (`{ playing: slot | null, ms }`) → the builder shows the `MOVE PREVIEW` pill over the stage while a clip runs.
   - Sprite-only vessels: `play` returns 0; the pill says `NO PREVIEW · SPRITE VESSEL`.
6. **Triggers** in the builder: node hover → `playSpell` after a 180 ms debounce, only if idle; node click / equip / twin pick → `playSpell` immediately (restarts); the technique panel's verb pill has a small `▶ PREVIEW` next to it for keyboard users. `state.animationsDisabled` and `window.EW_NO_PB_PREVIEW` disable the trigger (the pill says `PREVIEW OFF`).
7. Test: the chain helper is shared (`_castChainFor` appears at both call sites), `EWCharViewer` exposes `play` / `playSpell`, `PB_TABS` unchanged, `.pb-technique` present.

**Acceptance.** Tree legality identical (twin / wildcard / path / sealed);
keyboard-only equip works; every 3D-ready race plays a cast clip on hover
(the user checks a handful; RULE #1c); no new asset loads (the library bake
is the board's, cached per model).

**Risks.** A `def.noAnimLib` vessel loads per-character Meshy clip GLBs on
demand (the board's legacy path) — slower, fine. Long clips (`castTrap`
4 s at 0.25×) — the cap holds unless `opts.full`. The board's
`_maybeStartModelAnim` is untouched; only the chain table moves.

### 5.3 · Stage 3 — THE SPELL LIGHTS UP THE STAGE (VFX in the viewer)

**Goal.** Equipping or previewing a technique fires its real VFX around the
hero — the staging beats at the caster, the spell's own effect at a dummy
target two tiles to the side — and the monitor reacts (a colour grade, a
scanline roll, the DOOR click).

**Files.** three-vfx.js (`attach`) · three-vfx-effects.js (the stage
adapter) · three-renderer.js (viewer stage group, `ThreeVFX.tick` in
`_cvFrame`) · party-builder.js (the trigger, the CRT beat) · styles-base.css
(grades) · index.html (bump) · party-builder.test.js.

**Work.**
1. **`ThreeVFX.attach(parent)` / `detach()`** (three-vfx.js): re-parent every pooled object (`_spritePool`, `_worldMeshPool`, `_quadMeshPool`, `_globPool`, live particle clouds) from `_scene` to `parent` and remember the board scene; `detach()` puts them back. `init(scene)` stays single-shot; the battle renderer's `ThreeVFX.init(scene)` (three-renderer.js ~25314) is unaffected. Nothing is allocated twice.
2. **`VFX3D.stage`** (three-vfx-effects.js): `enter({ group, tilePx, heroH })` / `exit()` / `fire(intent, spellId, params)`. While staged: `_suppressed()` returns false regardless of `state.phase`; `tilePx` maps tile (0,0) to the hero's feet and (2,0) two tiles to screen-right in a frame whose "tile" is `tilePx` world units (the viewer's hero is ~1 unit tall; `_cfg()` returns `{ tileSize: 128, boardPadding: 0 }` and the **stage group is scaled `tilePx / 128`** so every px-authored size lands right); `unitSurfaceZ` → 0; `unitZBoost` → `heroH × 0.5` in px; `window.shakeBoard` → a CRT jolt callback instead; `_spell3DGeometry[id]` builders and `ThreeLightning` are SKIPPED unless tagged `stageSafe` (they reach into the board renderer). **`stage.fire` calls the internal `fire` implementation directly — never `window.VFX3D.fire`, which online.js wraps for the host → guest relay** (rule 3.6b). Add a source-scan assertion that party-builder.js contains no `VFX3D.fire(`.
3. **The viewer side** (three-renderer.js): `_cvEnsure` adds `v.vfxGroup` (a `THREE.Group` at the origin); `_cvFrame` calls `ThreeVFX.tick(dt)` while `v.staged`; `EWCharViewer.stageEnter()` / `stageExit()` wrap `ThreeVFX.attach(v.vfxGroup)` + `VFX3D.stage.enter(...)`; `unmount()` always exits the stage (so the pools are back in the board scene before any match starts — `startMatch` → `_unmountReactPartyBuilder` already runs first). A faint 5×3 tile grid decal (phosphor, 8 % opacity) under the hero so AoE footprints read.
4. **The beat script** (`EWCharViewer.previewSpell(spell, opts)`): yaw the hero to face screen-right (tween `v.yaw` → −90° over 200 ms; restore after), `stage.fire('windup', id, { sx:0, sy:0, tx:2, ty:0 })`, `playSpell` (Stage 2), at 45 % of the clip `stage.fire('burst', …)` then the mapped intent(s) at the target: `aura` for self / ally casts at (0,0), else `impact` / `bolt` / `beam` / `aoe` / `chain` at (2,0) (`hasMapping` decides; `descent` / `wall` / `teleport` at (2,0) too — they are the fun ones), `finish` at the end. One preview at a time; a new one clears the live particles (`ThreeVFX.clear()` if present, else let them die).
5. **The monitor reacts** (party-builder.js + styles-base.css): `.ms-crt-forge[data-grade="<spellType>"]` for 600 ms — six grades reusing the ENTROPY STRIKE typed palettes (styles-cinematic.css `.ews-t-<id>`: human / alien / divine / unholy / tech / anomaly); a scanline roll (`.pb-crt-roll`); `playDoorSfx('crtOn', { volume: 0.3 })` on equip, `'stamp'` on CONFIRM (already), the jolt class on `shakeBoard`. Gated by `state.animationsDisabled` / `EW_NO_PB_PREVIEW`.
6. Test: `attach` / `detach` exist, `VFX3D.stage` exists, the viewer exposes `stageEnter` / `stageExit` / `previewSpell`, no `VFX3D.fire(` in party-builder.js.

**Acceptance.** A preview in the builder shows the same particles the Spell
Library lab shows for that spell; no particle survives into the next match;
an online room's guest never receives a preview relay (host + guest both in
the builder — nothing appears on the other screen); kill-switch works.

**Risks — the highest of the plan.** three-vfx-effects.js is 23 k lines of
board assumptions. The adapter overrides the four coordinate helpers and one
gate; anything that reaches `ThreeRenderer.*` directly stays off the stage
(the `stageSafe` whitelist grows by inspection, effect by effect). **Stage 2
is the shipped fallback**: if the adapter slips, the animation-only preview
stands and this stage retries.

**As shipped (rev 4 — what differs from the work list above).** The
inspection found that the effects module reaches the board renderer in only
three places (`tileZ` / `unitSurfaceZ` ground truth, the ACTION TILE GLOW
`_glowR`, one `getMiscModelClone` for the moon) and every mesh it builds
goes through `ThreeVFX._getScene()` + `_worldPos` — so the bespoke 3D
geometry FOLLOWS the pools onto the stage by construction and is allowed
there by default (`window.EW_PB_VFX_NO_GEOM` makes it board-only) instead
of a per-effect `stageSafe` whitelist. Three helpers route the remaining
board reach: `_shake(kind)` (every `window.shakeBoard(` call — on the
stage it becomes the `fx('shake')` callback → the viewer camera's jolt +
the CRT jolt), `_LT()` (every lightning-module call — a no-op stub on the
stage; the module draws into the board scene) and `_geom3D(id)` (every
`_spell3DGeometry[id]` read). `_post()` returns a shim on the stage whose
`spellGrade` / `spellGradeKick` / `dramaDim` become `fx('grade')` /
`fx('kick')`; `_sigScreenFlash` becomes `fx('flash')`; `_sigSpeedLinesFx`
(a full-viewport canvas) is skipped; `_sigCasterPos` answers (0,0); the
board's burning tiles / rain / ambient clouds never tick on the stage.
`ThreeVFX.attach(group)` INITIALISES the pools when the builder opens
before the first match (`_home` = null) and `init(scene)` ADOPTS them at
the board's init; `detach()` calls `clear()` and parks the objects in
`_home` or an off-scene `_limbo` group. The kill-switch is
`window.EW_NO_PB_VFX` (→ the Stage 2 animation-only preview);
`EW_NO_PB_PREVIEW` still kills both.

### 5.4 · Stage 4 — ROSTER: the champ-select grid + the rounding pass

**Goal.** The ROSTER tab is the Smash / Mortal Kombat wall: a grid of
portrait tiles, hover shows the vessel on the stage, click seats it in the
active slot. Plus the sweep that rounds every remaining square.

**Files.** party-builder.js · styles-base.css · index.html (bump) ·
party-builder.test.js.

**Work.**
1. `.pb-roster`: fixed-size tiles (`--pb-r` corners, `aspect-ratio: 1`, the 128×128 portrait when `getUnitPortraitUrl` has one else the sprite bottom-anchored, a 2px faction ring on hover / active, three tiny type dots, ★ favourite, 🔒 → `_goToShop(race)` as today), one flat grid (decision C-4) sorted by `sortKey`, faction bands as thin coloured rules between groups when sorted by faction.
2. Hover → `EWCharViewer.setCharacter(race, gender)` after 220 ms (cached GLBs swap instantly; uncached ones show the summoning ring, as the codex does); leaving the grid → back to the slot's vessel. Click → `pickRace(race, gender, job)` (unchanged) → the party ring pulses → TECHNIQUES (C-1).
3. Filters as round chips: faction ×3 (coloured rings), type ×6 (circles with the type glyph, `TYPE_C`), the job dropdown → a `.pb-window` list, search as a pill input, sort as a pill menu. `filteredRoster` / `rosterEntries` logic unchanged.
4. The right pane on ROSTER: the stage + a card under it: name · race · job · type chips · four quick pills (HP ATK DEF SPD) · `CONFIRM n` (`confirmSlot`).
5. **The rounding pass** (rule 3.2) across every element still square after Stages 1–3: `EquipSlotBox` → circles; `SpellBlade` rows; the locker's team cards and mini portraits; the picker rows; the codex chips; every `clipPath` polygon removed. Stamps and diamonds excepted.
6. Test: `.pb-roster` present, no `clipPath` left in party-builder.js except inside `door-` markup.

**Acceptance.** Unlock gate, favourites, filters, P2 (CPU) team editing,
standalone NEW TEAM all behave as before.

### 5.5 · Stage 5 — STICKY NOTES, STAT PILLS, AFFINITIES, GEAR, DOSSIER

**SHIPPED 2026-09-09 (rev 6) — see §9.** Two deviations from the list below, both deliberate: the notes stick on a WIDENED RIGHT BEZEL (not the corners — `#builderOverlay` clips anything past the monitor's edge and the bottom-left corner wears the label), and the AFFINITIES read `strongVs` / `weakVs` (the engine's rule), not the documentary `resists` field. The ZODIAC wheel states the ENGINE's rule (there is no `ZODIAC_NATURES` table — there never was).

**Goal.** The dressing: passives as paper notes on the bezel, the stats as
the references draw them, the affinity ring, and the two quiet tabs.

**Files.** party-builder.js · styles-base.css · index.html (bump + one
Google-Fonts family for the handwriting, decision C-6) ·
party-builder.test.js.

**Work.**
1. **Sticky notes** (`.pb-notes`, a layer over the BEZEL, outside the glass; corners top-right and bottom-left like the references, a third at bottom-right if needed): one note per passive from `getUnitPassives({ race, gender, … })` (a pseudo-unit built from the slot's identity — it resolves `flying` first, the race's ids after, capped like the engine), paper colour per faction (pink / yellow / cream), rotation seeded by the race id (±3°), a folded corner, the handwriting face. Text = `PASSIVE: <name>` (underlined) + `desc` (Claude-written mechanics; user-authored marginalia = decision C-7). `RACE_TRAITS` terrain rules become a smaller `TERRAIN` note. Max three; overflow → a paperclip `+n` that opens a `.pb-window` list. Hover / focus lifts the note (`translate(-2px,-4px)`, a deeper shadow) and shows the full text. Notes never overlap the glass content (the bezel is wider since Stage 1).
2. **Stat pills** (`.pb-stat`): a 22px round disc (stat colour + a glyph: ♥ HP · ◆ MP · ✊ ATK · 🛡 DEF · ✦ M ATK · ◈ M DEF · ➶ SPD · ◉ AWR · ✧ CRT · ↯ EVA) · label · a rounded bar (`--pb-r-sm`) · the value; zodiac ▲▼ and subclass / gear deltas as today (`VitalBar` / `StatBar` restyled, `computeFullStats` untouched); the grade chip → a tiny ring; MOVE / RANGE diamonds inside round badges.
3. **AFFINITIES** (`.pb-affinity`): six circular type icons (`TYPE_C` colour, the type glyph). For the vessel's own type(s) the ring reads incoming matchups from `TYPE_CHART`: `WEAK` (red ring, ×1.30), `RESIST` (green ring, ×0.75), neutral (faint). Hover → `takes 1.3× from ALIEN`.
4. **GEAR tab**: two accessory circles + three item circles flanking the hero (the pickers unchanged), the SUBCLASS pill (opens the subjob picker; `handleSecJobChange` as today), NAME as a pill input, ZODIAC as a wheel of twelve round glyph chips (`ZODIAC_ICONS`, `handleZodiacChange`) with the nature's buff / debuff named under it.
5. **DOSSIER tab**: the customs file as it is (stamps stay square), the lore in Cormorant, the `TOP SECRET // NOFORN` foot.
6. Test: `.pb-notes` reads `getUnitPassives`, `.pb-affinity` reads `TYPE_CHART`, no `<select>` in party-builder.js except the sort menu (decision C-9).

**Acceptance.** Every number equals today's (same helpers); the notes show
`flying` for SKY_RACES and both passives for the werewolf; the wheel sets
the same `zodiac` value the `<select>` did.

### 5.6 · Stage 6 — THE DESK: the forge in the building, sounds, kill-switch

**Goal.** In the D.O.O.R. headquarters the builder runs on a real desk's CRT
(camera push, power-on) and the pre-match flow stays on ONE monitor: the
site terminal FILES the crossing, the same screen swaps to the forge, SEAL
leaves the building into the crossing cinematic.

**Files.** map.js (`_hqOpenTerminal` kind `forge`, the swap) · ui.js
(`renderBuilder` host choice) · party-builder.js (mount opts `{ host, frame,
variant }` like match-select) · data.js (the forge counter on a desk,
decision C-8) · styles-base.css (`.hq-terminal .ms-crt-forge`) · index.html
(bump) · doorhq.test.js + party-builder.test.js.

**Work.**
1. `window._mountReactPartyBuilder(opts)` accepts `{ host: 'builderOverlay' | 'hqTerminal' | 'teamBuilderBody', frame: 'page' | 'room', variant: 'match' | 'archive' }`; `frame: 'room'` = transparent bezel over the pushed camera (the `.hq-terminal` rules already do this for `.ms-crt`).
2. The one-monitor pre-match flow: when `_msConfirm` runs inside an open HQ terminal and the filed mode needs a party, map.js swaps programs instead of closing — `_hqTerminalSwap('forge')`: unmount match-select from `#hqTerminal`, mount the builder there (`frame: 'room'`, `variant: 'match'`; the building stays suspended, `_hqSuspend`); ui.js `renderBuilder()` mounts into `hqTerminal` when `window._hqTerminalIsOpen()`. `◂ BACK` swaps back to the SITE terminal (the crossing record is still `window._hqPreselect`). `SEAL` → `_hqTerminalClose({ launch: true })` → the existing start path (`doStart` unchanged, the return spot stays the console — `_hqLastDoor`). ESC = STEP AWAY (`_hqTerminalClose()`, power-down, `unfocus(560)`, `_hqResume`) — the walker's `onEscape` already checks `_hqTerm` first.
3. The standalone forge on a desk: a `crt_terminal` prop + a counter `{ id: 'forge', label: 'THE FORGE', sub: 'SQUAD MANIFESTS', verb: 'USE', action: { overlay: 'forge' }, radius: 1.5 }` in the room decision C-8 picks (recommend the OFFICE — your own desk, next to the in-tray); `_hqConsoleTerminal` routes `overlay: 'forge'` → `_hqOpenTerminal({ variant: 'archive', counterId: 'forge', kind: 'forge' })`; the Quartermaster door's `alt: PARTY BUILDER` keeps pointing at `_goToTeamBuilder` (the monitor on black) until the desk ships, then points at the desk. doorhq.test.js learns the counter (the room register is untouched — a counter wears no number).
4. Sounds: `uiCursorMove` on tab / row moves, `crtOn` on power-up (map.js already), `vhsEject` on power-down, `stamp` on CONFIRM, `dotMatrix` on SAVE TEAM, `identSting` on SEAL.
5. Kill-switch: `?nopbcrt` / localStorage `ew_pb_crt='off'` / `window.EW_NO_PB_CRT` → the same tabs and row without the bezel (a flat phosphor page). `EW_NO_PB_PREVIEW` (Stages 2–3) stays.
6. Docs: a CLAUDE.md paragraph ("THE FORGE TERMINAL"), a PLAYTEST_NOTES section (the `window.GAME`-side hooks a future playtest would drive: `PB_TABS`, `_pbSetTab`, the row keys), §9 here.

**Acceptance.** HQ: console → SITE → FILE → the forge on the same glass →
SEAL → the crossing; STEP AWAY at every step returns to the walk with the
pointer lock released (the dead-cursor rules in CLAUDE.md "THE TERMINAL").
`?nohq` route: the monitor on black, as Stage 1. `npm test` (doorhq +
party-builder) green.

## 6. Decisions awaiting the user (answer inline; Claude proceeds on the recommendation if unanswered)

| #   | Question                                                                                                  | Recommendation                                                                                              |
| --- | --------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| C-1 | Picking a vessel on ROSTER flips to TECHNIQUES automatically?                                             | Yes — that is the references' flow (select entity → techniques). `CONFIRM n` stays on every tab.            |
| C-2 | Tab names: ROSTER · TECHNIQUES · GEAR · DOSSIER, or STATUS · SKILLS · LOADOUT · ARCHIVE?                   | ROSTER · TECHNIQUES · GEAR · DOSSIER (the game already says "abilities/techniques", "gear", "dossier").     |
| C-3 | STATS: a persistent right column, or its own tab?                                                         | Persistent (every reference keeps the stats on screen); ROSTER shows a 4-stat quick read.                  |
| C-4 | The champ grid: one flat wall (Smash) or faction sections?                                                | One flat wall, sortable, faction rings + thin band rules when sorted by faction.                            |
| C-5 | Pillar taglines ("The Mind Reaches Further."): Claude drafts for approval, user writes, or none?           | Claude drafts a table (jobs + races) in a later session; until approved the line shows `n TECHNIQUES`.     |
| C-6 | The handwriting face for the sticky notes (one Google Fonts family, allowed by the CSP allowlist)?          | **TAKEN 2026-09-09 (Stage 5): `Caveat` 500 / 700** (index.html Google-Fonts link); swap the family name in index.html + styles-base.css `.pb-notes` to change it. |
| C-7 | Sticky-note marginalia (a second, user-voiced line under the mechanics — A15 rule: lines are user-authored)? | Data slot `PASSIVE_DEFS[id].note` (string) read if present; Claude never writes it.                        |
| C-8 | Which desk hosts the standalone forge in the HQ: the OFFICE (your desk, by the in-tray) or DISPATCH?       | The OFFICE — your manifest is filed from your own desk; DISPATCH's screen stays the crossing terminal.       |
| C-9 | Keep any native `<select>`? (sort menu is the last one)                                                   | **DONE**: the sort menu went in Stage 4, the zodiac select in Stage 5 — party-builder.test.js fails on any `h('select'`. |
| C-10| Preview trigger: hover previews the animation, click adds the VFX — or VFX on hover too?                   | **DECIDED by the user 2026-09-09: VFX on hover too** ("the vfx always play with the animation"). Hover still never cuts a beat already running. |

## 7. Keyboard (and the pad, when the bindings screen gets a builder page)

| Key                 | Action                                                 |
| ------------------- | ------------------------------------------------------ |
| `Q` / `E`, `[` / `]`| previous / next tab (L1 / R1)                          |
| `1`–`4`             | jump to a tab                                          |
| `←` / `→`           | previous / next party slot (the row)                   |
| `↑ ↓ ← →` (TECH)    | move across the circuit (arrows on the row only when the tree has no focus) |
| `ENTER`             | equip / pick / confirm the focused thing; on the foot = SEAL |
| `BACKSPACE`         | unequip the focused node                               |
| `SPACE`             | replay the preview                                     |
| `ESC`               | close a window → BACK / STEP AWAY                      |

## 8. Kill-switches and fallbacks

- `window.EW_NO_PB_CRT` / `?nopbcrt` / localStorage `ew_pb_crt='off'` — the flat page (Stage 6).
- `window.EW_NO_PB_PREVIEW` — no cast clips, no VFX (Stages 2–3); `state.animationsDisabled` implies it.
- `window.EW_NO_PB_VFX` — the animation plays, the stage never opens (Stage 2's preview); `window.EW_PB_VFX_NO_GEOM` — particles only, no bespoke 3D geometry on the stage.
- `window.EW_DISABLE_3D_UNITS` — the viewer already refuses; the sprite stays, the pill says NO PREVIEW.
- Stage 3 absent or failing → Stage 2's animation-only preview is the shipped behaviour.
- No React (`window.React` missing) → ui.js's legacy fallback path is unchanged.

## 9. Build log (append every session)

- **2026-09-09 — rev 1.** Plan written from the seven references and a read of party-builder.js, match-select.js, the `EWCharViewer` block, the VFX binding (`ThreeVFX.init` single-scene, `VFX3D.fire` relayed by online.js), `classifySpellAnimKind` + the renderer's cast chain, `PASSIVE_DEFS` / `getUnitPassives`, the HQ terminal plumbing. Nothing shipped. Next: Stage 1.
- **2026-09-09 — rev 2 · STAGE 1 SHIPPED (the monitor: tabs + the party row).** party-builder.js re-homed into the match-select CRT: root `.ms-crt.ms-crt-page.ms-crt-forge.pb-tarot` (`--pb-fc` kept), bezel → glass → screen → `.ms-tty.pb-tty` (HEAD · `.pb-tabbar` · `.pb-body[data-tab]` · `.pb-party` · `.ms-tty-foot.pb-foot`), `.ms-crt-label` = `D.O.O.R. · CUSTOMS & ADMISSIONS · FORGE-1 · DO NOT UNPLUG`. `PB_TABS` (module constant, `window.PB_TABS`; `window._pbSetTab(id)` for tooling), React state `pbTab`, opens on ROSTER, `pickRace` flips to TECHNIQUES (C-1 taken). Keys: Q / E and [ / ] cycle (the `.pb-tabcap` L1 / R1 caps click), 1–4 jump, ← → walk the row, ESC closes a window else backs out (pre-match `doBack`; standalone edit → the locker; locker → main menu) — never while an input has focus. Body grids per §5.1 item 5; the STAGE (`.pb-stage`, key `'stage'`) is rendered ONCE and placed by `grid-area` (the test counts one `HeroViewer3D`). Pieces moved unchanged inside: ROSTER ← the Codex filter row + `.pb-roster` wall; TECHNIQUES ← the abilities head (slots / RND / RST / CLR) + `SpellTreePanel` (+ the rack / flat-pool fallback) with the twin / socket picker as a `PbWindow` over the column; GEAR ← two accessory + three item `EquipSlotBox`es (now 50 % discs), the SUBCLASS bar (every non-Freelancer class — the tree's pillar head still opens the same picker), NAME, ZODIAC; DOSSIER ← the customs file; STATS column ← job / types / vitals / bars / MOVE-RANGE diamonds / RACE TRAITS (ROSTER shows a quick card under the hero: name · job · types · HP ATK DEF SPD · CONFIRM). PARTY ROW: `◂ ▸` caps, `.pb-party-slot` (portrait in a 50 % clip, 2 px faction ring, active = 3 px + glow + 1.12 scale, numeral disc, FILED stamp), P1 | P2 pills when `state.showPlayer2Builder` (local only; `selectPlayer` resets the slot to 0), `n / size FILED`. FOOT: BACK / TEAMS · summary (`SLOT II · name`, race · job · sub · spells n/6) · the prompt · 🎲 ONE / ALL / RESET · ★ SAVE / ↑ LOAD · CONFIRM n · SEAL / WAITING / START (conditions verbatim) — standalone: the name pill + 💾 SAVE TEAM. Windows: module-level `PbWindow({ title, sub, onClose, width, zone })` (a per-render component would remount on every keystroke) — the team modal, the equip / subjob / item picker, the node picker (`zone: true`). The locker is `.pb-locker`, a full-glass view with its own head / foot, team cards rounded. Removed: `StarField` (the glass is the ground), `OfficerChip` (→ `pbOfficer()` + `.ms-tty-officer`), the ASSESSMENT / DOSSIER `pbx-tab` pair (`heroTab`). CSS: styles-base.css "THE FORGE TERMINAL" block after "THE TERMINAL" (`#builderOverlay { position: relative }` because the CRT root is absolute; `--pb-r` / `--pb-r-sm`; 1180 / 940 px breakpoints — under 940 the body stacks stage → tech → stats and scrolls). Tests: NEW `party-builder.test.js` (8 source-scan tests; `npm test` = 178 pass). index.html `?v=20260909b-cors`. Not done here, by design: the technique panel + circuit (Stage 2), the roster wall tiles / native `<select>`s (Stage 4), sticky notes / stat pills / affinities (Stage 5). The legacy `.pb-*` CSS at styles-base.css ~1106–3150 is still untouched (§5.1 risks). Next: Stage 2.
- **2026-09-09 — rev 3 · STAGE 2 SHIPPED (TECHNIQUES: the circuit, the technique panel, the move preview) + the party row sized up.** *The row first (the user: "way too small"):* `.pb-party { --pb-portrait: 96px }` (76 under 1180 px, 64 under 940), the ring reads the token, active = 4 px ring × 1.16 + a 26 px glow, numeral disc 26 px, names 15 px Cormorant, the `◂ ▸` caps 40 px; the body above is the flex child that gives. *The circuit:* `SpellTreePanel` keeps its name, `TREE_NODE_POS` and every rule; the skin is round-capped `<path>` connectors (`.pb-circuit-edge` lit = phosphor / hover = gold / unlit = dashed), 50 px chips (ring-4 capstones 58 px inside a thin `.pb-node-crown` ring — the ♛ is gone), the twin ⇄ as a `.pb-node-twin` satellite disc, sockets dashed gold, sealed a 🔒 disc, the pillar heads as `.pb-pillar-head` pills (title + a tagline line = `n TECHNIQUES` until C-5; SUBCLASS is the `btn` pill) in their OWN strip above the board (`.pb-circuit` = heads 44 px · `.pb-circuit-board` (the % space) · `.pb-circuit-pips`). `nodeState` moved out to module-level `treeNodeState(tree, sealed, equipped, key)` (unchanged logic) so the panel and the keyboard share it. *Selection model:* `techSel` (click / keyboard — a gold halo, `selKey` / `onSelect`) + `techHover`; `treeStepKey(key, dir)` walks ↑ ↓ along a pillar, ← → across on the same ring, root ↔ ring 1. On TECHNIQUES the circuit OWNS the arrows (SHIFT+← → still walks the party row; every other tab: arrows = the row), ENTER = the verb, BACKSPACE = unequip, SPACE = replay the preview — never while a field or a button has focus. *The technique panel* (`TechniquePanel`, module-level, `.pb-technique` under the circuit): 44 px category disc · `RING n · TIER · CATEGORY · PILLAR` · the name (Cormorant) + type badge · desc (3-line clamp) · chips (AP / MP / RNG or SELF / AOE / PWR / n SLOTS + every `pbSpellEffects` row) · the verb pill: EQUIP / UNEQUIP / ⇄ PICK / ＋ BROWSE / PATH · +n NODES / NO ROOM · n SLOTS NEEDED / NO PATH / SEALED — CLASH RULES (sealing only exists in Clash) / ALWAYS EQUIPPED (root) · `▶ PREVIEW` (`■ PLAYING` while live, `▶ PREVIEW OFF` when gated) · `used / cap SLOTS`. `pbTechInfo(...)` computes it; `techVerb(info)` does exactly what the chip click does (`treeNodeClick` / the twin picker / the socket picker). The floating spell card no longer opens on circuit nodes (the panel is the card); it stays on the picker rows and the flat pool. *The move preview (three-renderer.js):* the board's two inline chain tables became `_castChainFor(kind)` / `_attackChainFor(kind)` (both board sites call them — the test insists); `_cvSetCharacter` now keeps the WHOLE bake (`v.clips = baked`, `v.clipUrls = def.clips` for `noAnimLib` defs, `v.actions` built lazily, `v.idleAct`, `v.def`), a mixer `finished` listener returns to idle; `EWCharViewer.play(slotOrChain, { full, name })` (LoopOnce, clampWhenFinished, the board's per-slot scale — `def.libTimeScales[slot]` → `UAL_SLOTS[slot].ts` → the Meshy `castTimeScale`, crossfade 0.12 s from idle, 0.2 s back, the 1.4 s cap unless `full`, one at a time), `playSpell(spell, { attack, full, name })` (`classifySpellAnimKind` → `_castChainFor`; a basic attack / `attack: true` → `def.basicAttackKind` → `_attackChainFor`), `stopPreview()`, `isPlaying()`, `hasClips()`, `onState(fn)` → `{ playing, ms, name }`. Sprite-only vessels return 0. *Triggers (builder):* `pbPreview(spOrNull, { hover })` — node hover / keyboard move after a 180 ms debounce and only when idle, click / equip / twin pick / wildcard equip / flat-pool equip immediately (restarts); `previewOff = state.animationsDisabled || window.EW_NO_PB_PREVIEW`. The stage wears `.pb-stage-pill`: `● MOVE PREVIEW · <name>` while a clip runs, `NO PREVIEW · SPRITE VESSEL` / `NO CLIP FOR THIS TECHNIQUE` for 1.4 s when a click cannot play, `PREVIEW OFF` on TECHNIQUES when gated. Picker rows: `.pbx-blade` rounded, slot numerals as discs. Tests: party-builder.test.js +4 (the shared chain at both sites and no inline table, the viewer API, the circuit / panel / triggers / no `VFX3D.fire(`, the row token ≥ 88 px) — `npm test` = 183 (182 pass, 1 skip). index.html `?v=20260909c-cors`. Not done here, by design: the VFX in the viewer (Stage 3 — `ThreeVFX.attach`, `VFX3D.stage`), the roster wall tiles / native `<select>`s (Stage 4), the sticky notes / stat pills / affinities (Stage 5), the tagline table (C-5). Next: Stage 3.
- **2026-09-09 — rev 4 · STAGE 3 SHIPPED (the spell lights up the stage: the VFX in the viewer).** *three-vfx.js:* `attach(parent)` / `detach()` / `isAttached()` — `_pooledObjects()` (sprites, world quads, billboard quads, globs, rain drops / splashes, the ambient clouds) re-parented by `_reparentAll` (`add()` detaches from the old parent, nothing allocated twice); `_home` = the board scene, `_attached` = the stage group, `_limbo` = an off-scene group for pools that have no board yet; a stage opened BEFORE the first match initialises the pools (`init(parent)` then `_home = null`) and the board ADOPTS them at its own `init(scene)` (the early-return now re-homes); `detach()` → `clear()` first (no particle survives into the next match); `isActive()` is true while attached (the effects' `_canSpawn` reads it); `_rainTick` / `_ambientTick` bail while attached; `dispose()` resets all three. *three-vfx-effects.js:* the `_VS` record + `_VS_CFG` (`{ tileSize: 128, tileGap: 0, boardPadding: 0 }`) — `_cfg` / `tileZ` (0) / `unitSurfaceZ` (0, never the stale `state.units`) / `_suppressed` (only `state.animationsDisabled`) / `_post` (the shim → `fx`) / `_glowR` (null) / `_sigCasterPos` ((0,0)) / `_sigScreenFlash` (→ `fx('flash')`) / `_sigSpeedLinesFx` (skipped) / `_syncTileFlames` + `_tickBurningTiles` (off) branch on `_VS.on`; the 13 `window.shakeBoard(` calls → `_shake(kind)`, the 18 lightning-module calls → `_LT()` (a no-op stub on the stage), the 13 `_spell3DGeometry[id]` reads → `_geom3D(id)` (allowed on the stage unless `EW_PB_VFX_NO_GEOM`); `VFX3D.stage = { enter({ tile, heroH, fx }), exit() (clearAll first), active(), fire(intent, spellId, params) (the INTERNAL fire — never the exported one online.js wraps), hasMapping, clear() }`. *three-renderer.js (EWCharViewer):* `v.vfxGroup` (scene child, scaled `tile / 128`, offset `-(64 − CONFIG.boardPadding) · s` on x/z and `−3 · s + 0.006` on y so the effects' px frame — `_vfxToWorld` subtracts the GLOBAL pad and adds the 3 px slab whatever the stage cfg says — lands tile (0,0) on the hero's feet; `tile = v.h / heightRatio` because a heightRatio-1 vessel is ONE tile tall on the board, `UNIT_SPRITE_SIZE_RATIO = 1.0`), `_cvFitStage` (re-run per vessel), `_cvBuildGrid` (the faint 5×3 phosphor grid, tiles −1..3 × −1..1), `_cvStageEnter` / `_cvStageExit` (attach + `stage.enter` / `stage.exit` + detach; `unmount()` exits first), `_cvFrame` ticks `ThreeVFX.tick(dt)` while staged, runs the yaw tween and the camera jolt; `_cvPreviewSpell(spell, opts)` = the beat: `ThreeVFX.clear()`, the hero turns to `_CV_STAGE_YAW` (1.15 rad toward +X = screen-right; the camera sits on +Z) over 200 ms, `windup` at (0,0) with `holdMs` = 45 % of the clip, `_cvPlaySpell` (Stage 2), at 45 % `burst` + every mapped intent the spell has (`aura` at the hero; `impact` / `bolt` / `beam` / `aoe` / `chain` / `descent` / `wall` / `teleport` at (2,0) — `_cvSpellAtSelf` puts range-0 / buff / heal / transform / summon / trap casts at (0,0)), `finish` at the clip's end, the turn back 420 ms later; a sprite vessel or a pending Meshy clip runs the VFX on a 900 ms clock; the player's drag cancels the turn. API: `previewSpell`, `stageEnter`, `stageExit`, `isStaged`, `onStageFx(fn)` (`(kind, o)` — grade / kick / flash / shake). *party-builder.js:* `pbPreview` — hover = `cv.playSpell` (C-10, animation only), click / equip / ENTER / ▶ = `cv.previewSpell`; the four equip sites pass `{ equip: true }` → `playDoorSfx('crtOn', { volume: 0.3 })`; `crtRef` on the CRT root + `previewSpellRef`; the `onStageFx` effect paints the monitor: `data-grade` = the technique's `spellType` for the beat's hold (`--pb-grade-amt` = tintAmt + dim / the flash peak), `.pb-crt-roll-on` on the kick, `.pb-crt-jolt` on the shake; two layers on the glass after the glare: `.pb-crt-grade` (a radial wash in the type's colour, `mix-blend-mode: screen`) and `.pb-crt-roll` (the scanline band). *styles-base.css:* the Stage 3 block after `.pb-stage-pill.off` — the six `[data-grade]` palettes (the ENTROPY STRIKE colours: human `#f2c468` · alien `#58d858` · divine `#ffd75a` · unholy `#ff3a2a` · tech `#4fd8ff` · anomaly `#ff4fa3`), `pbCrtRoll`, `pbCrtJolt` on `.ms-crt-glass`, reduced-motion keeps the wash only. Tests: party-builder.test.js +3 (the pools lend / adopt / release; the stage overrides + the internal fire + the three routers; the viewer API + the builder trigger + the CSS) — `npm test` = 186 (185 pass, 1 skip). index.html `?v=20260909d-cors`. Kill-switches: `EW_NO_PB_VFX` (Stage 2 preview), `EW_PB_VFX_NO_GEOM`, `EW_NO_PB_PREVIEW` (both). Not done here, by design: the roster wall tiles / native `<select>`s (Stage 4), the sticky notes / stat pills / affinities (Stage 5), the desk in the building (Stage 6), the tagline table (C-5). RULE #1c: not playtested — the user checks a handful of techniques (a bolt, an AoE, a self-buff, a summon) and reports the framing; `_CV_STAGE_YAW`, the (2,0) target and the 45 % burst are the three numbers to tune. Next: Stage 4.
- **2026-09-09 — rev 5 · THE CIRCLE, THE MOVE, THE FRAME, THE WALL (Stage 4 SHIPPED) + two bugs the probe found.** *The user's three asks first.* (1) **The row's portraits were OVALS** — the legacy "Character Select Screen v2" block (styles-base.css ~3467) still declares `.pb-party-slot` with a clamped HEIGHT, a border and a hover translate / glow; inside that column the ring's height shrank. The forge block now overrides all of it (`.ms-crt-forge .pb-party-slot, .pb-party .pb-party-slot { height: auto … }`, the hover / active reset, `.pb-party-ring { flex: none; aspect-ratio: 1 / 1 }`); the probe measures the rings at 96×96 / 111×111. (2) **The VFX plays with EVERY preview** — C-10 overruled: `pbPreview` stages hover too (`stageOn` no longer gated on `!opts.hover`; hover still never cuts a running beat; `EW_NO_PB_VFX` keeps the animation-only path). (3) **The hero MOVES and the camera FRAMES the beat** (three-renderer.js): `_cvMovePlan(spell, opts)` names the dummy's tile and the move — a charge (`chargeToTarget`, the tackle kind) RUNS to (2,0) beside its dummy at (3,0) at `_CV_RUN_MS_PER_TILE` = 240 ms a tile with the run / walk clip looped for the run (`_cvPlay` grew `opts.loop` / `opts.ms` and a direct handoff crossfade between clips) and STRIKES on arrival (the melee clip, a shake, burst + the mapped intents fired from the landing tile — `VFX3D.stage.caster(x, y)` moves the effects' caster anchor, `_sigCasterPos` reads it), a dash slides to its far tile with every tile on the line in `hitTiles`, a leap arcs (`arc`), a teleport / escape BLINKS out at 45 % and back in at (2,0), a melee swing / melee basic attack lunges 0.16 tile out-and-back at a dummy on (1,0); `_cvMoveTo` / `_cvMoveTweenStep` drive `v.heroX` / `v.heroY` (the stage group, the shadow blob and the sigil follow), the beat walks home 420 ms after the finish, `_cvBeatCancel` resets it (and never leaves a blinked hero invisible). THE FRAME: `v.frameTo = { x0, x1, y1 }` (world units — a tile behind the hero to half a tile past the last thing that happens, the AoE included) and `_cvFrame` pulls the camera out to `max(rest, halfW / tanH, halfH / tanV)` and slides its look-x to the centre (eased, `camDist` / `camLx`), then eases home when the beat clears; the player's wheel zoom stays the floor. The stage grid reaches tile 4. *Stage 4 — the wall:* `.pb-rtile` tiles (`--pb-r` corners, `aspect-ratio: 1` art — the 128×128 portrait full-bleed when `getUnitPortraitUrl` has one, else the sprite on the tile's floor — a faction ring on hover / active, three type dots, ★ on hover / when starred, 🔒 → the Shop), round filters (`.pb-type-disc` ×6 with `PB_TYPE_GLYPH`, `.pb-faction-ring` ×3, `.pb-pill-input` search, the SORT and JOB pills → `PbWindow` menus `.pb-menu-list` / `.pb-menu-row` — C-9: no native `<select>` on the wall; the GEAR zodiac select is Stage 5's), `✕ CLEAR` when anything filters; hover → `rosterHoverIn(entry)` (220 ms) → the stage follows (`stageRace` / `stageGender` / `stageCls` / `stageFaction` / `stageLabel` feed `HeroViewer3D` + the title reads `<vessel> · <job> · PREVIEW`), leaving the wall / a pick / leaving the tab restores the slot's vessel. `filteredRoster` / `rosterEntries` / `pickRace` / `isLockedEntry` / `toggleFav` unchanged. *Two pre-existing bugs the probe surfaced (both fixed):* the standalone flag `_pbStandaloneMode` was MODULE-level and the pre-match forge sleeping in `#builderOverlay` re-renders while the archive is open and flipped it — the standalone instance then ran as pre-match (its head read CUSTOMS & ADMISSIONS, ESC ran `doBack` into the match-select terminal); now `PartyBuilder(props)` reads `props.standalone` (each mount passes its own), and the key handler only acts for the instance ON SCREEN (`crtRef` inside `#teamBuilderPage.active` / no title page in front). And match-select.js's ENTER / ESC listener stays live after a visit (the root is kept) — it now returns unless its host has client rects (`props.hostId`). *Tooling:* NEW `playtest_builder.js` (repo tooling; the playtest_menu.js network shim; standalone route: main menu → PARTY BUILDER → NEW TEAM; shots/builder/<tag>_{roster,roster_hover,roster_sortmenu,roster_typefilter,tech,tech_preview}.png; prints the ring sizes, the hover title, the tile count after a filter, page errors). GLBs cannot load in the sandbox, so the move + the frame are verified by reading and by `npm test` only — the user checks Brave Charge (the run + the strike), a dash (Drive-By), a teleport, an AoE (the pull-out). Tests: party-builder.test.js +3 (the circle override, the move + frame + `stage.caster`, the wall) and two Stage 3 assertions retargeted — `npm test` = 189 pass. index.html `?v=20260909e-cors`. Not done here, by design: the sticky notes / stat pills / affinities / GEAR / DOSSIER (Stage 5), the desk in the building (Stage 6), the tagline table (C-5). Numbers to tune: `_CV_RUN_MS_PER_TILE` (240), the charge dummy at (3,0), the frame pads (`0.35 tile` horizontal, `0.2 tile` vertical), the lunge (0.16 tile / 350 ms). Next: Stage 5.
- **2026-09-09 — rev 6 · STAGE 5 SHIPPED (the sticky notes, the stat pills, the affinity ring, GEAR, DOSSIER).** *The notes* (`pbUnitNotes(identity, cls, equipment)` → `PbNotes`, module-level): a pseudo-unit (race / gender / job / types / gear, an empty status map) goes through data.js `getUnitPassives`, so `flying` resolves exactly as on the board (map.js `canFly` — the SKY_RACES, the Psychic, the telepath, a jetpack in either gear slot) and the race's ids follow, capped at MAX_UNIT_PASSIVES; the hand-authored `RACE_TRAITS` rows that are NOT registry passives become one smaller TERRAIN note (three rows on the paper, `📎 +n MORE` past that); a user-voiced marginalia line rides `PASSIVE_DEFS[id].note` when present (C-7 — never written by Claude). Paper per faction (`PB_NOTE_PAPER`: chaos pink · time yellow · space cream), ±3° seeded by the race id (`pbNoteRot`), a strip of tape, a folded corner, `Caveat` (C-6 taken; `family=Caveat:wght@500;700` added to index.html's Google-Fonts link), hover lifts the note and drops the line clamp, a click opens THE NOTES window (`notesOpen`, counted in `anyWindow` so ESC closes it). **Where they stick**: the plan said the bezel CORNERS — but `#builderOverlay` is `overflow: hidden` (anything past the monitor's edge is clipped) and the bottom-left corner wears the paper label — so the forge's RIGHT bezel is widened by `--pb-notes-w` (132 px; `.ms-crt-forge .ms-crt-bezel` / `.ms-crt-glass` read it) and the notes hang in that margin, never over glass content. Under 1180 px the margin closes (`--pb-notes-w: 0px`), the notes hide and the same text reads inline in the STATS column (`.pb-traits-inline`, `display: none !important` otherwise). The locker hides them. *The stat pills* (`StatBar` / `VitalBar` keep their names + a `statKey` prop; `PB_STAT_LOOK` = colour + glyph per stat: ♥ HP · ◆ MP · ✊ ATK · 🛡 DEF · ✦ M ATK · ◈ M DEF · ➶ SPD · ◉ AWR · ✧ CRT · ↯ EVA): `.pb-stat` = a 22 px disc (18 compact) · label (+ the zodiac ▲▼) · a `--pb-r-sm` bar filled in the stat's colour (the vitals keep the nameplate fills) · the grade as a 16 px ring (`GradeChip` → `.pb-grade-ring`) · value · delta; `computeFullStats` / `statGrade` untouched — the ROSTER quick card inherits the pills. MOVE / RANGE diamonds sit inside `.pb-foot-badge` round badges (§3.2: the diamond IS the rule's shape). *AFFINITIES* (`pbAffinities(types)` → `PbAffinityRing`, under the footprints, replacing the RACE TRAITS list): six discs in the ENTROPY STRIKE order; for the vessel's own types the ring reads INCOMING hits the way state.js `getTypeDamageMultiplier` does — attacker type A: `TYPE_CHART[A].strongVs` meets ours (and `weakVs` does not) → WEAK, red ring, ×1.30; `weakVs` meets ours (and `strongVs` does not) → RESIST, green ring, ×0.75; both → neutral; the vessel's own type is filled (STAB on its casts); hover → `takes 1.3× from ALIEN`. (The plan said `resists` — that field is documentation; the engine reads strongVs / weakVs, and the test insists.) *GEAR*: the gear + item discs 64 px, the SUBCLASS is a `.pb-sub-pill` (same `setEquipPicker('subjob')` → `handleSecJobChange`), NAME stays the pill input, ZODIAC is a wheel of twelve `.pb-zodiac-chip` discs (`ZODIAC_ICONS`, coloured by element, `handleZodiacChange` on click — the `<select>` is gone, C-9 done) with the sign's ENGINE rule under it: `PB_ZODIAC_ELEMENT` mirrors battle.js Star Crossed's `_zElementOf` (the test diffs the two tables), the read = "when <sign> rules the sky (5 rounds in every 60): +10 % move & armor" (state.js `getZodiacBonus`) + what Star Crossed does to that element (burn / root −1 DEF / silence / −1 M ATK, 50 % harder under its own sky). `ZODIAC_NATURES` never existed in data.js — the old "buff ▲ · debuff ▼" label always read null; the variable stays (harmless) for `zMod`. *DOSSIER*: unchanged, checked (the stamps stay square, the NOFORN foot). Tests: party-builder.test.js +5 (the notes' engine source + the bezel + the margin + Caveat; the pills + badges; the affinity rule; the wheel ↔ battle.js table, no `<select>`, the pill; the dossier) — `npm test` = 194 (193 pass, 1 skip). index.html `?v=20260909f-cors`. RULE #1c: not playtested — the user checks a flyer (fairy: Flying + Pixie Dust Trail notes + the Daywalker / Forest Spirit TERRAIN note), the werewolf (Lycanthropy + Bloodcraze), a jetpack on a ground race (a Flying note appears), the pills' colours, an alien vessel's ring (WEAK to human, RESIST to divine), and a zodiac click. Numbers to tune: `--pb-notes-w` (132), the note size (118 px), the 1180 px cut. Not done here, by design: the desk in the building + the sounds + the flat-page kill-switch (Stage 6), the tagline table (C-5). Next: Stage 6.
- **2026-09-09 — rev 7 · THE RELAYOUT (the user: "a lot of empty space", "the skill tree is all smushed together", "the post-it notes need to be ON the screen", "the skill tree window transparent so we see the environment", "the nodes redesigned — they don't convey enough", "cut the fluff").** *The glass is the screen:* the right bezel no longer widens for the notes (`--pb-notes-w` is 0; the token stays for the test) — `PbNotes` renders INSIDE `.pb-stage-view`, a column at the top-right of the hero's band (`.pb-notes { right: calc(var(--pb-stats-w) + 14px) }`), over the scene; the inline `.pb-traits-inline` copy is gone. *The stage under everything:* on TECHNIQUES / GEAR / DOSSIER `.pb-stage` spans every column (`grid-column: 1 / -1; grid-row: 1`, z 0) and the two side zones float transparent over it with a soft scrim (`.pb-zone-tech::before` / `.pb-zone-stats::before`); ROSTER keeps its panels. Columns are `--pb-tech-w: 36%` / `--pb-stats-w: 21%` and the hero stands at the band's centre: `PB_STAGE_CX = 0.575` → `HeroViewer3D { focus }` → NEW `EWCharViewer.setFocus(cx)` (three-renderer.js — `v.focusX`; `_cvFrame` slides the camera AND its look point by `(0.5 − fx) · 2 · dist · tan(fov/2) · aspect`, and while a beat's `frameTo` fills the width `v.camFx` eases back toward 0.5 so a charge never runs off the canvas). `--pb-cx` on the stage places the glow, the floor, the pill, the sprite fallback and the MANIFESTING ring at the same x. The future ground + sky go in the viewer and will fill the whole body. *The circuit is THREE LANES:* `SpellTreePanel` (same name, same `TREE_NODE_POS` keys, same legality + `treeStepKey`) now renders `.pb-lanes` → three `.pb-lane`s (head pill, then ring 4 → ring 1 as NODE ROWS `.pb-tn` = category disc `.pb-tn-disc` + NAME + one META line `pbNodeMeta` — the type glyph disc, MP, R<n> / SELF, AOE, power, `n SLOTS`, the twin's `⇄ or <alternate>`) joined by CSS `.pb-link` segments (lit / hover-gold / dashed), each dropping onto a `.pb-bus` whose centre cell holds the root hub. States are classes (`is-equipped` white-lit, `is-reachable` full colour, `is-far` dim, `is-blocked` / `is-sealed` dark dashed, `is-empty`, `is-socket`, `on-path`, `sel`, `cap` ring, `daat`). The SVG `<path>` connectors and the `.pb-node-label` under-chip text are gone. Rows scale with the viewport (`--pb-row: clamp(46px, 6.6vh, 64px)`, `--pb-disc`); the circuit never shrinks under its rows (`flex: 1 0 auto` — the scroll container scrolls instead). *One head row:* the tabs (`.pb-tabbar`) ride inside `.pb-head` between `ENTROPY WARS · FORGE-1` and `n SLOTS · MODE · ESC`; the officer chip, `D.O.O.R. ▸`, `CUSTOMS & ADMISSIONS · VESSEL ASSIGNMENT` and the tab hint line are cut. *One bottom bar:* the foot merged into `.pb-party` — `.pb-party-left` (BACK / ◂ TEAMS + `.pb-sum` name · race · job (+ sub) · n/n SPELLS + the P1 / P2 pills), the portraits, `.pb-party-right.ms-tty-foot.pb-foot` (`.pb-tools` = 🎲 ONE / 🎲 ALL / RESET / ★ SAVE / ↑ LOAD, `.pb-seal` = CONFIRM + the START / WAITING / SEAL ladder verbatim, or the name input + SAVE TEAM). The `> forge --slot` prompt, the ◂ ▸ caps and the `n / size FILED` counter are cut. *Other cuts:* the stage title (the identity moved to the STATS column's `.pb-ident`: name, RACE · JOB + SUB, the type chips + faction), `ABILITIES 6/6 SLOTS` + `THE CIRCUIT … HOVER PREVIEWS` → one `.pb-tech-bar` (TECHNIQUES · pips · RND / RST / CLR), `AFFINITIES · WHAT IT TAKES` → `AFFINITIES`, `MOVE (SPD)` → `MOVE`, the panel's kicker drops the pillar name, the empty panel's hint is one line. Breakpoints: ≤1500 px tighter slots / tools as a column / CONFIRM over the seal; ≤1180 px lanes lose the meta line; ≤940 px stacked, the stage centred. Tests: party-builder.test.js rewritten for the lanes / the glass notes / the spanning stage (a tie: `PB_STAGE_CX` must equal the CSS band centre) / the one head + one bar; `npm test` green (196). playtest_builder.js clicks `.pb-tn` rows, photographs the hover path and GEAR, prints the geometry; `PW_W` / `PW_H` set the viewport. Screenshots checked at 1920×1000 and 1440×820 (RULE #1c — the user asked for visual verification this session). index.html `?v=20260909g-cors`. Still owed: the ground + sky in the viewer (the user's next ask), the tagline table (C-5), Stage 6.
- **2026-09-09 — rev 8 · THE PANEL BESIDE THE PORTRAITS (the user: "move the portraits closer / smaller so the techniques section runs all the way down and the spell description sits next to the portraits — the tree is still cramped").** The body grid grew a second row: `grid-template-rows: minmax(0, 1fr) auto`, areas `"tech band stats" / "party party party"`, and on TECHNIQUES with a tree (`.pb-body[data-panel="1"]`, `hasPanel` in party-builder.js) `"panel party party"` — `TechniquePanel` renders in its own `.pb-zone.pb-zone-panel` cell UNDER the lanes (same scrim), the lanes take the whole sheet height (`.pb-circuit-scroll` no longer shares it with the panel), and the party bar (`.pb-party`, now a grid cell with `grid-area: party`, rendered inside `.pb-body`) sits beside the panel. The stage spans BOTH rows (`grid-row: 1 / -1`) so the scene runs under the bar too. Portraits: `--pb-portrait` 84 (72 ≤1500, 64 ≤1180), gap 8, slot width portrait + 22, active ×1.12; the summary (`.pb-sum`) left the bar (the identity is the STATS column's), `.pb-party-left` is BACK / ◂ TEAMS + the P1 / P2 pills; `.pb-tools` is a column of small pills at every width; `.pb-party-slots` is the flex child that gives, `.pb-party-right` never shrinks. Tests: the tty is `head, body`; `panelZone, partyRow` close the body; the `data-panel` areas rule; the portrait floor is 72. Screenshots at 1920×1000 / 1440×820. index.html `?v=20260909h-cors`.
- **2026-09-09 — rev 9 · TYPE CHART + THE ELEMENTS (the user: "add the elemental weakness/resistances — that's what I mean by affinities; the type chart can just be called the type chart, and below it the elemental info").** The STATS column's `AFFINITIES` heading is now **TYPE CHART** (`PbAffinityRing` / `pbAffinities` unchanged — still TYPE_CHART strongVs / weakVs, ×1.30 / ×0.75). Under it a second ring, **ELEMENTS**: `pbElementAffinities(race)` reads data.js `RACE_ELEMENT_AFFINITY` through `getRaceElementAffinity` (the engine's `unitElementAffinity` is the same read off the unit's race) for the six `COMBAT_ELEMENTS` (fallback `PB_ELEMENT_ORDER`, the test diffs it against data.js) and hands `PbElementRing({ race })` a verdict per element — weak ×1.5 (red ring) · resist ×0.5 (green) · immune ×0 (ice-blue, "its status bounces") · absorb (gold, "the hit heals") · neutral '—' (the common case). The discs wear the element art via `elementIconHtml(el, 'pb-elicon')` (R2 `Assets/Icons/elements/`, emoji fallback) — `.pb-element` reuses the `.pb-aff` classes plus new `.immune` / `.absorb` rings (styles-base.css). party-builder.test.js "ELEMENTS" guards the read, the order, the four tiers, the heading order and the CSS. index.html `?v=20260909i-cors`.
- **2026-09-09 — rev 10 · THE STRIKE FRAME (the user committed `rigged_animations/`: "view these to give better animations to the abilities and time the vfx with them better").** Every library clip was rendered to a contact sheet (repo tool `anim-sheets.js`, PLAYTEST_NOTES "THE STRIKE FRAME") and every action slot in sprites.js `UAL_SLOTS` now carries `strikeAt` (the source second the hit lands) and, where a clip has dead time, `trim`. *The stage:* `_cvPreviewSpell` fires the burst + the mapped intents ON that frame (`_cvStrikeMs`, via `_cvSpellChain` — the same chain `_cvPlaySpell` plays), the blink-out lands on it, a charge's arrival hits wait for it; the 45 % guess survives only for rigs without a library bake. *The board* got the same frame (battle.js `_releaseCastSprite` / doAttack leads) so the preview and the match agree by construction. New body language: castRanged = the trimmed cowboy draw, dodge = Roll, castDash / castTackle (Sword_Dash / Shield_Dash) for the move kinds. `anim-strike.test.js` guards the table.

- **2026-09-10 — Character creator (implemented locally; not deployed).** GEAR → CHARACTER CREATOR adds opt-in appearances for Homosapien slots using the uploaded male/female Meshy base GLBs. The existing singleton stage supplies rotation, full-body/face framing, and the shared walk animation. Controls: height/build, chest/waist/hips, head/jaw/nose/cheeks, skin color, bald/crop/crest hair, fitted bodysuit/T-shirt/tank with trousers, and hair/top/trouser colors. These bases have one skin each, no textures/materials, and no authored morph targets: face and body shaping is procedural and intentionally bounded; clothing and hair are starter shells cut from the base topology, not finished art assets. Geometry, colors and materials are instance-owned and disposed with their preview/battle rig. Body triangles covered by clothing are removed from the body draw to prevent poke-through. Cosmetics persist through existing team presets, party-meta repair, identity resolution and createUnit; ordinary party-config/state-sync carries them to the other player. Nonhuman races keep their existing models. Original models can be restored per slot. New controls respect online lock state. Portraits remain the existing race art; team saves use the existing local profile store, not a new cloud service.
  R2 location verified as `Assets/Models` (capital M). Browser-style R2 requests returned no Access-Control-Allow-Origin for the tested origin, so the loader retries these two allowlisted GLBs through `/api/character-model/:gender`; server.js serves only the corresponding files under rigged_animations, never the directory. Deploy server.js with index.html and retain both GLBs in the Render repository. Failed preview loads show RETRY; unmount invalidates stale asynchronous callbacks. Seven new creator tests cover input bounds, model selection, identity/preset/online wiring, route restrictions, fallback loading, and the actual r128 CPU skinning of both bases across 27 combinations each. `npm test`: 256 total, 255 passed, 1 existing server-boot skip (server dependencies absent), 0 failures; all 12 inline entry-page scripts syntax-checked. Offline geometry contact sheet reviewed; no browser/gameplay playtest performed. Cache token: `20260910-creator-075900-cors`. Complete-file delivery: sprites.js, three-renderer.js, party-builder.js, styles-base.css, state.js, map.js, profile.js (R2); server.js/index.html and the two base GLBs (Render); character-creator.test.js, updated party-builder.test.js and docs (repository only).

- **2026-09-10 — Character creator visual iteration 2 (local delivery; not deployed).** Replaced triangle-centroid masks with interpolated triangle clipping for necklines, sleeves, trouser hems and hairlines. Cut vertices merge bone influences and normalize the strongest four weights. Clothes now have relaxed geometry, chest panels that bridge anatomical valleys, more clearance, turned-in edges, subtle waistband/hem shading and a matte fabric material. Crop and crest have shaped crowns, swept volume, directional ridges and a contoured front/side/back hairline. Body and cloth normals are recomputed before cutting to preserve smooth lighting. Per-instance GPU buffers are disposed before topology replacement. Both preview and battle still use the same appearance generator and unchanged saved appearance IDs. Full npm test: 256 total, 255 passed, 1 existing server-start skip, zero failures; actual male/female assets exercised across 27 combinations each, including animated skinning, normalized generated weights, valid indices, finite normals and instance isolation. Front/side/back offline geometry sheets inspected; no browser/gameplay playtest. This is improved procedural art, not an authored garment or hair asset library. Cache token: 20260910-creator2-084120-cors. Updated runtime files for this iteration: R2 three-renderer.js; Render index.html. Package also includes the preceding creator files for a complete installation.
- **2026-09-11 — Character creator rev 3: the `charactercreation/` assets (local delivery; not deployed).** The user's new folder (repo + R2 `Assets/Models/charactercreation/`): two 30k-tri UV-unwrapped Meshy bases (male / female — **unrigged**, no skeleton), the HunterHairs pack (`hair-pack-part-1/source/HairPackPT1.glb`, 15.9 MB, 14 styles each on its own physics armature with diffuse + normal textures) and 16 tileable 1024² fabric base-colour PNGs. **Tooling** `character-rig.js` (repo-only): `bodies` rigs each new base by transferring the old rigged donor's 24-joint skin weights onto it (same Meshy body, same A-pose — verified by overlay render; nearest-surface barycentric blend, two smoothing passes, top-4 normalised; the donor node tree / skin / animation kept verbatim, the new geometry placed in its metre-scale bind frame) → `Meshy_AI_human_body_base_mesh_<gender>_rigged.glb` (1.2 MB each); `hair` splits the pack into one static GLB per style, rest-pose skinned, centred on the style's scalp cap, textures embedded, `asset.extras.ewHair` → `hair/hairNNN.glb` (1.1–1.8 MB each). Posed renders of the rigged output match the donors bone for bone. **Data** (sprites.js): appearance v2 — `hair` = `bald` | a catalogue id (v1 `crop`/`crest` map to hair003/hair000), `beard` none/stubble/goatee/beard, `outfit` tee/tank/suit(long sleeve), `bottoms` trousers/shorts, `topFabric`/`bottomFabric` (17 keys incl. `plain`), `eyeColor`, `lipColor`, `eyeSize`, `brows`; catalogues `EW_HAIR_STYLES` (provisional labels), `EW_FABRICS` (repeat / roughness / metalness per tile), `EW_APPEARANCE_ENUMS`; `getHairStyleUrl` / `getFabricTextureUrl` / `getCharacterAppearanceAssets`; `randomCharacterAppearance`; every asset has ONE same-origin fallback (server.js `/api/character-model/{male,female}`, `/hair/:id`, `/fabric/:key`, strict allowlists). **Renderer** (three-renderer.js, the "CHARACTER CREATOR RUNTIME" block): the body wears a PAINTED skin texture — skin tone + eyes (sclera / iris / pupil / highlight / lash line / lid shadow on the sculpted eyeballs found by `_ccFaceLandmarks`, the depth-weighted socket centroid: male ±0.0295 / 0.9415, female ±0.0287 / 0.9384 in q units), brows, lips with a cupid's bow, facial hair, blush — rasterised per texel through the base's own UVs (`_ccBakeSkin`, 1024², ~30 ms, re-baked only when a face field changes); garments are two shells (top / bottom) cut from the relaxed body with the fabric tileable through the same UVs (`_ccLoadFabric`: luminance-normalised, 512², RepeatWrapping) tinted by VERTEX colour; hair = the style GLB's parts fitted to the SKULL (scale = skull width / depth ÷ the scalp cap, top-aligned, refits with the head slider), skinned 100 % to the Head bone, the diffuse turned into a grey mask (`_ccHairTexture`) tinted by vertex colour, alpha-cut, `_ew_noTwin` (no x-ray / outline hull). Geometry is INDEXED (base vertices shared, only cut vertices appended) with whole-triangle fast paths — a full shape rebuild fell from 1.7 s to ~0.2 s, colour changes ~25 ms; the stage coalesces slider storms to one rebuild per frame. **The seam lesson:** the unwrapped base splits vertices along every UV seam; relaxing / averaging normals per index tore every seam open (black cracks all over the shirt) — both now run over position-WELDED vertices. The board's Lambert swap carries side / alphaTest / transparent; the match-start preload gate warms the hair GLB + fabric tiles. state.js `resolveIdentityForBuild` keeps a creator look's male / female choice (the stock human 3D model is male-only, which used to snap FEMALE back). **UI** (party-builder.js GEAR → CHARACTER CREATOR): BODY (MALE / FEMALE, height / build / chest / waist / hips) · FACE (head / jaw / nose / cheeks / eye size / brow weight, eye + lip swatches with custom pickers, facial hair) · SKIN · HAIR (15 tiles + colour) · CLOTHES (top cut, fabric tiles with live thumbnails from the stage's own tiles via `EWCharViewer.fabricThumb`, tint; bottoms cut, fabric, tint) · RANDOMIZE / RESET / USE ORIGINAL MODEL · FULL BODY / FACE CLOSE-UP / PREVIEW WALK. **Verified** in headless Chromium (`node playtest_creator.js`, the creator assets served from disk): both bases dressed and idling, eyes / brows / lips / beard on the face, Long straight / Ponytail / Mohawk on the skull, denim + chainmail tiles, the female base, a random look, the walk — no page errors. `character-creator.test.js` (10 tests) covers the data model, catalogue ↔ disk, fallbacks, routes, the rig generator on the real files and the tool. Not deployed: the user uploads `charactercreation/*_rigged.glb` + `charactercreation/hair/*.glb` to R2 `Assets/Models/charactercreation/` (the fabric tiles are already there), plus sprites.js / three-renderer.js / party-builder.js / styles-base.css / state.js to R2, index.html + server.js to Render. Cache token `20260911-creator3-charactercreation-01-cors`. **Still procedural, still missing (asset wishlist for a robust creator):** a skin albedo / normal map per base (the bake would layer over it), authored face morph targets (eye shape / nose / mouth), real eyeball + teeth meshes, eyebrow / lash cards, garment MESHES (shirts / jackets / trousers / boots as skinned GLBs on the same rig) instead of cut shells, fabric normal / roughness maps, more hair (a part-2 pack goes through the same tool), a beard mesh pack, and body morphs (muscle / weight) — see the delivery note.

**2026-09-11 — CHARACTER CREATOR rev 4: the shirt drape, the face shell, the eyes (local delivery).** The user's three complaints — shirts "way too boxy", "every bump and crevice" in the shading, eyes "too far apart / really bad" — traced (headless renders, see `creator-render.js` below) to: (1) the chest PLANK: the shirt front was pushed out to a per-row maximum across the whole torso width (`chestRows` / `rowsS`), a flat plate with a vertical crease at |x| = 0.09 — replaced by **THE DRAPE** (`drapeFront`: a 5 mm grid of the relaxed cloth's front depth, each row's UPPER CONVEX HULL between the torso's edges — cloth spans the sternum valley and the hollow under a collarbone, a convex cross-section is its own hull so the sides stay round — then a column drape downward with a maximum fall slope of 0.3 so a shirt hangs from the bust / pecs instead of hugging under them; ease 0.009 → 0.007, relaxation 10 → 14 passes); (2) the raw 30k-tri surface: vertex normals of every shell are now relaxed over the welded one-ring (`smoothNormals`, 3 passes at 0.6 on the body and the cloth, 0.32 on the face so the nose / lips keep their edges) and the skin is matte-ish (roughness 0.62 → 0.8); (3) the eye landmarks: the bases' sockets are a 3–5 mm dip, far too faint for the old depth-weighted recess search, which settled on the deep OUTER corners by the temples (±0.0295 q = a 10 cm pupil distance); `_ccFaceLandmarks` is now ANTHROPOMETRIC — ±0.53 × the front half-width at eye level, 53 % of the way from the chin to the crown, both read off the mesh (male ±0.0195 / 0.940, female ±0.021 / 0.937). **THE FACE SHELL:** the head triangles (t > `CC_HEAD_T` 0.856) are a fourth shell (`EWCreator_face`) with its OWN cylindrical UVs (`_ccFaceUv`: u = the angle round the head, seam at the back — seam triangles re-emitted with their own vertices at u + 1, RepeatWrapping; v = height) and a 2048 × 1024 painted texture (`CC_FACE_TEX`, half on mobile): the base's atlas gave the head ~9 texels/cm scattered over islands with a seam through one eye; the face now gets ~35 texels/cm and no seam. The body shell wears the skin tone as VERTEX colour and no texture. The painter (`faceColor`) was rewritten allocation-free with band gates (the array-literal version cost 1.7 s per bake in the same-realm timing; a repaint is now ~265 ms, a reshape 85–170 ms — the vm-sandboxed test harness reports ~15× that because vm global lookups are interceptor-slow, don't trust its numbers) and the rasteriser skips triangles outside the feature zone (the back of the head, the crown). THE EYES are an almond (a flatter upper lid, a shallower lower one, the outer corner a touch higher), the iris as tall as the opening with its top under the lid (relaxed, not wide-eyed), warm corners, a lid shadow, a limbal ring, one key-light highlight on the same side in both eyes, a wet line, soft ~2-texel edges everywhere (hard edges aliased), a full upper lash line + a faint lower one + a lid crease; brows ~5 mm, closer to the eye, solid with the faintest grain (the sine "hairs" read as a barcode / hatching); lips 5.3 cm wide and thinner, mixed at 0.55 / 0.5; the mouth-bag darkening needs z < −0.008 (it was painting the under-jaw skin at z 0.015–0.03 as two black patches); the BEARD MASK follows the jaw (top edge rising from the mouth corners to the sideburns, the jaw's underside by down-facing normals, stops at the jaw angle and the ears, a soft lip hole; goatee = chin wedge + a mouth-wide moustache) — the old rule was a band at mouth height out to the ears. **`creator-render.js`** (repo tooling, needs `three@0.128.0`): builds the rig like the test does and software-rasterises body / top / bottom / the face shell WITH its baked texture (a stub 2D canvas) to PNG — `VIEWS=torso,torso34,side,head,head34,eyes,full`, `CROP=1` dumps a texel crop round each eye, `RENDERER=` renders another copy for before / after — so a creator change can be LOOKED AT without a browser (RULE #1c intact; every render above was made with it). `character-creator.test.js` now expects four shells (body + top + bottom + face). Cache token `20260911-creator4-face-drape-01-cors`. Not deployed: three-renderer.js to R2, index.html to Render; `creator-render.js`, the test and these notes to the repo. Still open: the tank / tee collar rim shows a dark zigzag where the inward rim meets a lumpy neck; the ears carry no paint (fine); the wishlist above stands.

**2026-09-11 · CHARACTER CREATOR rev 5 — the face off the mesh, the skull map, the cloth frame, the welded weights (local delivery).** The user's four complaints, each traced to its cause with `creator-render.js` (extended: fabrics + hair + `back` / `top` / `hair*` views, see PLAYTEST_NOTES) and the Playwright probe: **(1) THE MOUTH.** `CC_FACE` claimed both bases share a face "to the millimetre" — they do not: the male's mouth line (the crease between the lips on the centreline depth profile) is at t 0.8985, the female's 0.8915, the nose tips 0.918 / 0.912; the table's 0.889 painted the male's lips a centimetre down his chin. `_ccFaceLandmarks(q, nz, ids)` now rasterises the front of the head into a 0.5 mm depth map (q frame, front-facing triangles) and reads the features off it: nose tip = the centreline's high point; mouth = the plateau-tolerant local minimum nearest 1.9 cm(q) under it; subnasale / mentolabial sulcus = the minima above and below the lips (lip heights = 0.40 / 0.55 of those gaps, clamped); chin = where the centreline stands proud of the throat; nasion = where the bridge's fall flattens (dz/dt > −0.1 — a plain minimum ran up the male's receding forehead); the eyes 3 mm(q) under the nasion, held to 50–56 % of chin → crown (male 0.9377, female 0.9336), ±0.53 × the front half-width at eye level; mouth half-width 0.48 × the face's half-width at the mouth; brow ridge = the eye column's high point (male 0.9455, female 0.9445). Everything lands in `lm.F` (the painter reads `lm.F || CC_FACE`; the constants are the fallback for an unreadable head) — 45 ms per base, cached. **(2) THE BROWS** sit ON the ridge (`F.browT`, clamped 8–13 mm(q) over the eye) and run from just inside the inner corner to 5 mm past the outer one (2.6 cm(q) ≈ 4.5 cm; rev 4's arc was 3.3 cm(q) and wrapped to the temples), thick at the head, the arch two thirds out, the tail a third of the head's height. **(3) THE HAIR.** Two faults: the fit centred the style on the skull's vertex CENTROID (denser on one side → every style a few mm off-axis: one temple bald, the other buried) — now the skull's box centre; and the pack's styles were modelled round the HunterHairs head, not either base — top-aligned cards dipped under the scalp at the temples and the crown. `buildSkullMap` (rebuildGeometry) makes the head a radial height field (64 × 32 azimuth × elevation bins from a centre at ear level, t ≥ 0.875, holes filled, smoothed) and `fitHair` pushes every card vertex to ≥ 4.5 mm off it, CONFORMS the scalp cap to +1.5 mm (pulled in or out, above ear level) and TUCKS the cap's rim below the nape hairline under the skin (the packs' caps run down the neck as a flat maroon panel — pre-existing, visible in the before render). Under any style the crown / back of the head is painted in the hair colour (`scalpOn` in `_ccBakeSkin`: a soft hairline high on the forehead, lower over the ears, lowest at the nape; 0.62 alpha, 0.45 for light hair; the bake's `inZone` covers the whole head then; the paint runs BEFORE the mouth-bag `z < −0.008` return, which is the whole back of the head) — the pinholes between cards show hair, not skin, and a mohawk's shaved sides read as stubble. `paintKey` carries bald ↔ hair. **(4) THE CRACKS** were two things. (a) The fabric read as a shattered patchwork because it tiled through the Meshy atlas — hundreds of small islands, each its own orientation. THE CLOTH FRAME: garment UVs are cut like cloth, in METRES — five centred polyline cylinders (`polyline` / `limbNearest` / `limbUv` / `clothRegion` / `clothUvOf` / `centreLimb` at the rig scope; joints from the skeleton's inverse bind matrices, `boneHead`, with geometric fallbacks): the torso (waist → chest → neck base → head, angle 0 = front, the seam down the spine), each arm (shoulder → elbow → wrist, the seam under the arm; the arm REGION starts at |x| > 0.155 q — past the deltoid and the armpit, where the skin hugs the bone and a cylinder swirls, so a tee's sleeve is torso-projected), each leg (a hand above the hip → hip → knee → ankle, angle 0 outward, the seam on the inseam; the WHOLE trousers incl. the pelvis — the torso axis runs through the crotch). Each polyline is centred in the flesh by the mean radial offset of its region's vertices (hands / feet / head excluded), and x-scaled by the Build slider per rebuild. A polygon that straddles two cylinders or a cylinder's seam is re-emitted with its own vertices in the majority projection (`fixClothUv`, `emitClothTri`); a CUT vertex re-projects from its position (`mix` interpolated its uv — garbage across the seam). `EW_FABRICS.repeat` is now tiles PER METRE of cloth (`_ccLoadFabric`). (b) The real cracks — thin dark lines on the shins, light ones on the shirt, everywhere a seam runs, only in the BROWSER: the weight transfer that rigged the bases gave the two copies of a seam vertex DIFFERENT weights (2,835 pairs per base, up to 0.48 apart), so a posed rig tears at every UV seam; the bind pose (every headless render) never shows it. The parts setup now WELDS the skin weights (per position group: bone weights averaged, top four, renormalised → `part.siW` / `part.swW`; the source attributes untouched) and every shell reads them. Verified in the browser (`playtest_creator.js rev5b`): no streaks, the mohawk's sides tinted, the female's face on her sculpt. `character-creator.test.js` pins the measured tables (per base, ±2.5 mm at the mouth, the bases' noses differ), the cap outside the skull, cloth v in metres, no garment triangle spanning half a torso turn; `npm test` 598 / 598. Cache token `20260911-creator5-face-hair-cloth-01-cors`. Not deployed: three-renderer.js to R2, index.html to Render; `creator-render.js`, the test and these notes to the repo. Still open: `EW_FABRICS.repeat` values were tuned for the atlas — now that they are tiles/metre the user may want denim coarser (12 → 8) and cotton finer; the elbow / knee angle reference shifts slightly at the joint (a real seam sits there anyway); the pack's scalp cap tints with the hair colour, never the skin.

**2026-09-11 · CHARACTER CREATOR rev 6 — the sleeves, the straps, nine tops (local delivery).** The user's two complaints — tee sleeves "all messed up and not even around the arms (the opening looks ripped)", the tank "really bad, specifically the straps over the shoulders" — and a request for more tops (male vs female tank, crop top, V-neck, crew, bikini top, jacket). Traced with `creator-render.js` (new: `VIEW='{"name","yaw","cx","ct","scale"}'` for one custom close-up in q units, `NO_HEMS=1` to render the bare cut edges) and fixed in three-renderer.js's "CHARACTER CREATOR RUNTIME": **(1) THE SLEEVE** was a vertical x-plane (`sleeve − |x|`) through an arm that hangs diagonally in the A-pose — a slanted ellipse that slewed further once the arm dropped — and the ragged look was two things at once (see 3). Sleeves are now cut SQUARE across the arm: `armAt(part, q)` gives the metres along the arm's centred polyline from the shoulder joint and the distance from it (clamped — a capsule), and the cut is `max(len − along, perp − 7.8 cm)` — a plane normal to the arm axis, gated to the arm — so the opening is a ring round the arm in every pose (tee 15.5 cm, crop 7.5 cm, 'wrist' = the arm minus a 1.2 cm cuff). **(2) THE STRAPS.** The old tank was `|x| < 0.093` plus the tee's collar on front AND back: a 7 cm band hugging the neck with a straight vertical armhole and a scoop on the back. A sleeveless top now drops the arm by the rig's own SKIN WEIGHTS — every vertex carries ARMNESS (the Arm / ForeArm / Hand bones' welded weight, `part.arm`) as q[3], interpolated by `mix` and read by the cuts (`0.5 − q[3]`); measured on both bases: the front deltoid is torso-weighted (Spine / Shoulder) while on the back the arm's influence starts at the rear armpit, exactly where a tank's back armhole runs — so a SCOOP (a superellipse in |x|, t centred past the shoulder joint, `part.shoulder.x` + dx; `sq: true` = exponent 4, a rounded rectangle whose inner edge runs straight down the front) shapes the front armhole and the strap's outer edge; the strap's inner edge is the NECK opening: separate front / back centre depths blended by `frontness(q)` (the ridge at z ≈ −0.01 counts as front), rising to 0.852 by |x| = w as a quarter-ellipse (crew / scoop: flat at the centre, vertical at the side) or a line (v; `top` caps a stand collar). **(3) THE TEETH.** Every opening was sawtoothed, worse than the mesh — with the hem strips OFF too. Cause: the cloth relaxation (14 Laplacian passes) also SLID vertices across the surface, up to a centimetre on the sliver-heavy back, while the cuts are evaluated in the bind frame (q) — a straight q-line landed on drifted positions (the bikini's two x-plane straps had 2 cm teeth). The relaxation now moves each vertex along its normal ONLY (`RN`, the welded body normals — the creases still iron out, nothing slides; the cloth UVs are truer for it). Two more: `split` refines every crossing with four secant / bisection steps (the cuts are curves; the plane's linear crossing was off by the bend — the tank's neckline had 5 mm steps at its vertical sides), and the bikini's front-only cup gate was a clamped constant (−0.001) that flattened the interpolation and tore the band's top edge on the back — it is a real depth distance now. **THE HEM**: a rim edge is found by `on` masks (the cuts a vertex lies on — `split(poly, cut, bit)`, `mix` intersects the masks; rev 5 tested |cut| < 5e-5, true only for a plane), the strip is planar (the mean of the two normals, ease + 4 mm long) and wears its own normal pointing OUT of the opening (down the cut's numeric gradient, else away from the polygon) — a hem lit as a hem, not a serrated dark band. **THE TOPS** = `CC_TOPS` (renderer) ↔ sprites.js `EW_OUTFIT_STYLES` (id + label; `EW_APPEARANCE_ENUMS.outfit` derives; the builder's Top row reads it): `tee` T-shirt (crew 0.806 / 0.828, w 0.048) · `vneck` V-neck (0.758, w 0.056) · `suit` Long sleeve (the v1 id; cuff shading) · `jacket` (stand collar to 0.848 with a front V, ease 11 mm, hem 0.548 over the waistband, a darker zip line down the front, a hem band, cuffs) · `crop` Crop top (cap sleeve, hem 0.678, hem band) · `tank` Tank top (scoop neck 0.778 / 0.818, w 0.06; front scoop rx 0.045 — 5 cm straps) · `atank` Athletic tank (the A-shirt: neck 0.752 / 0.80, w 0.05, deep rounded armholes to t 0.72 — 4 cm straps) · `racer` Racerback (the tank's front, a true ellipse on the back centred inside the joint, rx 0.07 → the straps run into a 3.5 cm centre panel — a Y) · `bikini` Bikini top (a band all round at t 0.70–0.728, triangle cups on the front from the centre 0.742 up to the apex (0.058, 0.80) and out to 0.105, 1.8 cm(q) straps over the shoulder at the apex' |x|, ease 4 mm). Both bases share the table; the scoops key off the shoulder joint so the female's narrower frame fits (her straps come out ~3.5 cm). The BOTTOMS hang at their own ease (`CB` / `NCB`, 7 mm): the jacket's 11 mm put the trousers' waistband on its hem's surface and they z-fought. `gainAt(target, q, shade)` now shades per top (hem line / hem band / zip / cuff). **Verified**: headless (every top, both bases: square sleeve rings, clean straps and necklines, the racerback Y, the bikini's straight straps) and in the browser (`playtest_creator.js <tag> --tops`: every top on both bases posed, plus the walk — see the note below). `character-creator.test.js` (12 tests) dresses every catalogue top on both bases at the shape limits and source-scans the catalogue ↔ CC_TOPS, the arm-axis sleeve, the q[3] weights, the normal-only relaxation and the rim masks. Cache token `20260911-creator6-tops-01-cors`. Not deployed: three-renderer.js, sprites.js, party-builder.js to R2; index.html to Render; `creator-render.js`, `playtest_creator.js`, the test and these notes to the repo. Still open: the tops are cut shells, so a jacket is a long sleeve with a collar and a zip line (no lapels, no open front — that needs a second top layer); the bikini shows on the male base too (no gender gate, on purpose); `randomCharacterAppearance` picks any top; hems are a 1 cm strip, not a fold.
